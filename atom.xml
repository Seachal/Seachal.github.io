<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Seachal的Blogs</title>
  
  <subtitle>记录学习的技能和遇到的问题</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://seachal.me/"/>
  <updated>2018-01-02T06:22:17.000Z</updated>
  <id>https://seachal.me/</id>
  
  <author>
    <name>Seachal</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title></title>
    <link href="https://seachal.me/uncategorized/2018/01/02/HenCoder/HenCoder%20Android%20%E5%BC%80%E5%8F%91%E8%BF%9B%E9%98%B6:%20%E8%87%AA%E5%AE%9A%E4%B9%89%20View%201-2%20Paint%20%E8%AF%A6%E8%A7%A3/"/>
    <id>https://seachal.me/uncategorized/2018/01/02/HenCoder/HenCoder Android 开发进阶: 自定义 View 1-2 Paint 详解/</id>
    <published>2018-01-02T06:18:11.000Z</published>
    <updated>2018-01-02T06:22:17.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="HenCoder-Android-开发进阶-自定义-View-1-2-Paint-详解"><a href="#HenCoder-Android-开发进阶-自定义-View-1-2-Paint-详解" class="headerlink" title="HenCoder Android 开发进阶: 自定义 View 1-2 Paint 详解"></a>HenCoder Android 开发进阶: 自定义 View 1-2 Paint 详解</h1><p>这期是 HenCoder 自定义绘制的第二期： <code>Paint</code>。如果你没看过第一期，可以先去看一下第一期：</p><p><a href="http://hencoder.com/ui-1-1" target="_blank" rel="noopener">HenCoder Android 开发进阶：自定义 View 1-1 绘制基础</a></p><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><p>上一期我已经简单说过， <code>Canvas</code> 的 <code>drawXXX()</code> 方法配合 <code>Paint</code> 的几个常用方法可以实现最常见的绘制需求；而如果你只会基本的绘制， <code>Paint</code> 的完全功能的掌握，能让你更进一步，做出一些更加细致、炫酷的效果。把 <code>Paint</code> 掌握之后，你几乎不再会遇到「iOS 组可以实现，但你却实现不了」的绘制效果。</p><p><img src="images/52eb2279ly1fig6d7kcppj207807t0tv.jpg" alt=""></p><p>由于依然是讲绘制的，所以这期就没有介绍视频了。绘制的内容一共需要讲大概 5~6 期才能讲完，也就是说你要看 5~6 期才能成为自定义绘制的高手。相对于上期的内容，这期的内容更为专项、深度更深。对于没有深入研究过 <code>Paint</code> 的人，这期是一个对 <code>Paint</code> 的诠释；而对于尝试过研究 <code>Paint</code> 但仍然对其中一些 API 有疑惑的人，这期也可以帮你解惑。</p><p>另外，也正由于这期的内容是更为专项的，所以建议你在看的时候，不必像上期那样把所有东西都完全记住，而是只要把内容理解了就好。这期的内容，只要做到「知道有这么个东西」，在需要用到的时候能想起来这个功能能不能做、大致用什么做就好，至于具体的实现，到时候拐回来再翻一次就行了。</p><p>好，下面进入正题。</p><p><code>Paint</code> 的 API 大致可以分为 4 类：</p><ul><li>颜色</li><li>效果</li><li>drawText() 相关</li><li>初始化</li></ul><p>下面我就对这 4 类分别进行介绍：</p><h2 id="1-颜色"><a href="#1-颜色" class="headerlink" title="1 颜色"></a>1 颜色</h2><p><code>Canvas</code> 绘制的内容，有三层对颜色的处理：</p><p><img src="images/52eb2279ly1fig6dcywn2j20j909yabu.jpg" alt=""></p><blockquote><p>这图大概看看就行，不用钻研明白再往下看，因为等这章讲完你就懂了。</p></blockquote><h3 id="1-1-基本颜色"><a href="#1-1-基本颜色" class="headerlink" title="1.1 基本颜色"></a>1.1 基本颜色</h3><p>像素的基本颜色，根据绘制内容的不同而有不同的控制方式： <code>Canvas</code> 的颜色填充类方法 <code>drawColor/RGB/ARGB()</code> 的颜色，是直接写在方法的参数里，通过参数来设置的（上期讲过了）； <code>drawBitmap()</code> 的颜色，是直接由 <code>Bitmap</code> 对象来提供的（上期也讲过了）；除此之外，是图形和文字的绘制，它们的颜色就需要使用 <code>paint</code> 参数来额外设置了（下面要讲的）。</p><p><img src="images/52eb2279ly1fig6gxcusnj20iw04xmzr.jpg" alt=""></p><p><code>Paint</code> 设置颜色的方法有两种：一种是直接用 <code>Paint.setColor/ARGB()</code> 来设置颜色，另一种是使用 <code>Shader</code> 来指定着色方案。</p><h4 id="1-1-1-直接设置颜色"><a href="#1-1-1-直接设置颜色" class="headerlink" title="1.1.1 直接设置颜色"></a>1.1.1 直接设置颜色</h4><h5 id="1-1-1-1-setColor-int-color"><a href="#1-1-1-1-setColor-int-color" class="headerlink" title="1.1.1.1 setColor(int color)"></a>1.1.1.1 setColor(int color)</h5><p>方法名和使用方法都非常简单直接，而且这个方法在上期已经介绍过了，不再多说。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">paint.setColor(Color.parseColor(<span class="string">"#009688"</span>));  </span><br><span class="line">canvas.drawRect(<span class="number">30</span>, <span class="number">30</span>, <span class="number">230</span>, <span class="number">180</span>, paint);</span><br><span class="line"></span><br><span class="line">paint.setColor(Color.parseColor(<span class="string">"#FF9800"</span>));  </span><br><span class="line">canvas.drawLine(<span class="number">300</span>, <span class="number">30</span>, <span class="number">450</span>, <span class="number">180</span>, paint);</span><br><span class="line"></span><br><span class="line">paint.setColor(Color.parseColor(<span class="string">"#E91E63"</span>));  </span><br><span class="line">canvas.drawText(<span class="string">"HenCoder"</span>, <span class="number">500</span>, <span class="number">130</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6di0nzij20js05pab1.jpg" alt=""></p><blockquote><p><code>setColor()</code> 对应的 get 方法是 <code>getColor()</code></p></blockquote><h5 id="1-1-1-2-setARGB-int-a-int-r-int-g-int-b"><a href="#1-1-1-2-setARGB-int-a-int-r-int-g-int-b" class="headerlink" title="1.1.1.2 setARGB(int a, int r, int g, int b)"></a>1.1.1.2 setARGB(int a, int r, int g, int b)</h5><p>其实和 <code>setColor(color)</code> 都是一样一样儿的，只是它的参数用的是更直接的三原色与透明度的值。实际运用中，<code>setColor()</code> 和 <code>setARGB()</code> 哪个方便和顺手用哪个吧。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">paint.setARGB(<span class="number">100</span>, <span class="number">255</span>, <span class="number">0</span>, <span class="number">0</span>);  </span><br><span class="line">canvas.drawRect(<span class="number">0</span>, <span class="number">0</span>, <span class="number">200</span>, <span class="number">200</span>, paint);  </span><br><span class="line">paint.setARGB(<span class="number">100</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);  </span><br><span class="line">canvas.drawLine(<span class="number">0</span>, <span class="number">0</span>, <span class="number">200</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><h4 id="1-1-2-setShader-Shader-shader-设置-Shader"><a href="#1-1-2-setShader-Shader-shader-设置-Shader" class="headerlink" title="1.1.2 setShader(Shader shader) 设置 Shader"></a>1.1.2 setShader(Shader shader) 设置 Shader</h4><p>除了直接设置颜色， <code>Paint</code> 还可以使用 <code>Shader</code> 。</p><p>Shader 这个英文单词很多人没有见过，它的中文叫做「着色器」，也是用于设置绘制颜色的。「着色器」不是 Android 独有的，它是图形领域里一个通用的概念，它和直接设置颜色的区别是，着色器设置的是一个颜色方案，或者说是一套着色规则。当设置了 <code>Shader</code> 之后，<code>Paint</code> 在绘制图形和文字时就不使用 <code>setColor/ARGB()</code> 设置的颜色了，而是使用 <code>Shader</code> 的方案中的颜色。</p><p>在 Android 的绘制里使用 <code>Shader</code> ，并不直接用 <code>Shader</code> 这个类，而是用它的几个子类。具体来讲有 <code>LinearGradient</code> <code>RadialGradient</code> <code>SweepGradient</code> <code>BitmapShader</code> <code>ComposeShader</code> 这么几个：</p><h5 id="1-1-2-1-LinearGradient-线性渐变"><a href="#1-1-2-1-LinearGradient-线性渐变" class="headerlink" title="1.1.2.1 LinearGradient 线性渐变"></a>1.1.2.1 LinearGradient 线性渐变</h5><p>设置两个点和两种颜色，以这两个点作为端点，使用两种颜色的渐变来绘制颜色。就像这样：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Shader shader = new LinearGradient(<span class="number">100</span>, <span class="number">100</span>, <span class="number">500</span>, <span class="number">500</span>, Color.parseColor(<span class="string">"#E91E63"</span>),  </span><br><span class="line">        Color.parseColor(<span class="string">"#2196F3"</span>), Shader.TileMode.CLAMP);</span><br><span class="line">paint.setShader(shader);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6dq7wudj206l06875e.jpg" alt=""></p><blockquote><p>设置了 <code>Shader</code> 之后，绘制出了渐变颜色的圆。（其他形状以及文字都可以这样设置颜色，我只是没给出图。）</p><p>注意：在设置了 <code>Shader</code> 的情况下， <code>Paint.setColor/ARGB()</code> 所设置的颜色就不再起作用。</p></blockquote><p>构造方法：<br><code>LinearGradient(float x0, float y0, float x1, float y1, int color0, int color1, Shader.TileMode tile)</code>。</p><p>参数：<br><code>x0</code> <code>y0</code> <code>x1</code> <code>y1</code>：渐变的两个端点的位置<br><code>color0</code> <code>color1</code> 是端点的颜色<br><code>tile</code>：端点范围之外的着色规则，类型是 <code>TileMode</code>。<code>TileMode</code> 一共有 3 个值可选： <code>CLAMP</code>, <code>MIRROR</code>和 <code>REPEAT</code>。<code>CLAMP</code> （夹子模式？？？算了这个词我不会翻）会在端点之外延续端点处的颜色；<code>MIRROR</code>是镜像模式；<code>REPEAT</code> 是重复模式。具体的看一下例子就明白。</p><p><code>CLAMP</code>:</p><p><img src="images/52eb2279ly1fig6e7vbemj20cj090goh.jpg" alt=""></p><p><code>MIRROR</code>:</p><p><img src="images/52eb2279ly1fig6egtxw5j20ck08xjv6.jpg" alt=""></p><p><code>REPEAT</code>:</p><p><img src="images/52eb2279ly1fig6em2wabj20ck08xjvo.jpg" alt=""></p><h5 id="1-1-2-2-RadialGradient-辐射渐变"><a href="#1-1-2-2-RadialGradient-辐射渐变" class="headerlink" title="1.1.2.2 RadialGradient 辐射渐变"></a>1.1.2.2 RadialGradient 辐射渐变</h5><p>辐射渐变很好理解，就是从中心向周围辐射状的渐变。大概像这样：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Shader shader = new RadialGradient(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, Color.parseColor(<span class="string">"#E91E63"</span>),  </span><br><span class="line">        Color.parseColor(<span class="string">"#2196F3"</span>), Shader.TileMode.CLAMP);</span><br><span class="line">paint.setShader(shader);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6ewf1o5j206d066q4a.jpg" alt=""></p><p>构造方法：<br><code>RadialGradient(float centerX, float centerY, float radius, int centerColor, int edgeColor, TileMode tileMode)</code>。</p><p>参数：<br><code>centerX</code> <code>centerY</code>：辐射中心的坐标<br><code>radius</code>：辐射半径<br><code>centerColor</code>：辐射中心的颜色<br><code>edgeColor</code>：辐射边缘的颜色<br><code>tileMode</code>：辐射范围之外的着色模式。</p><p><code>CLAMP</code>:</p><p><img src="images/52eb2279ly1fig6f2jz23j20ck08yach.jpg" alt=""></p><p><code>MIRROR</code>:</p><p><img src="images/52eb2279ly1fig6fad0cpj20cm09142x.jpg" alt=""></p><p><code>REPEAT</code>:</p><p><img src="images/52eb2279ly1fig6fflc56j20cl090tdr.jpg" alt=""></p><h5 id="1-1-2-3-SweepGradient-扫描渐变"><a href="#1-1-2-3-SweepGradient-扫描渐变" class="headerlink" title="1.1.2.3 SweepGradient 扫描渐变"></a>1.1.2.3 SweepGradient 扫描渐变</h5><p>又是一个渐变。「扫描渐变」这个翻译我也不知道精确不精确。大概是这样：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">Shader </span><span class="keyword">shader </span>= new <span class="keyword">SweepGradient(300, </span><span class="number">300</span>, Color.parseColor(<span class="string">"#E91E63"</span>),  </span><br><span class="line">        Color.parseColor(<span class="string">"#2196F3"</span>))<span class="comment">;</span></span><br><span class="line">paint.setShader(<span class="keyword">shader);</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword">...</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword">canvas.drawCircle(300, </span><span class="number">300</span>, <span class="number">200</span>, paint)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6fmbemdj206u061my4.jpg" alt=""></p><p>构造方法：<br><code>SweepGradient(float cx, float cy, int color0, int color1)</code></p><p>参数：<br><code>cx</code> <code>cy</code> ：扫描的中心<br><code>color0</code>：扫描的起始颜色<br><code>color1</code>：扫描的终止颜色</p><h5 id="1-1-2-4-BitmapShader"><a href="#1-1-2-4-BitmapShader" class="headerlink" title="1.1.2.4 BitmapShader"></a>1.1.2.4 BitmapShader</h5><p>用 <code>Bitmap</code> 来着色（终于不是渐变了）。其实也就是用 <code>Bitmap</code> 的像素来作为图形或文字的填充。大概像这样：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">Bitmap </span><span class="keyword">bitmap </span>= <span class="keyword">BitmapFactory.decodeResource(getResources(), </span>R.drawable.<span class="keyword">batman); </span> </span><br><span class="line"><span class="keyword">Shader </span><span class="keyword">shader </span>= new <span class="keyword">BitmapShader(bitmap, </span><span class="keyword">Shader.TileMode.CLAMP, </span><span class="keyword">Shader.TileMode.CLAMP); </span> </span><br><span class="line">paint.setShader(<span class="keyword">shader);</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword">...</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword">canvas.drawCircle(300, </span><span class="number">300</span>, <span class="number">200</span>, paint)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6fragq2j20lc089djv.jpg" alt=""></p><blockquote><p>嗯，看着跟 <code>Canvas.drawBitmap()</code> 好像啊？事实上也是一样的效果。如果你想绘制圆形的 <code>Bitmap</code>，就别用 <code>drawBitmap()</code> 了，改用 <code>drawCircle()</code> + <code>BitmapShader</code> 就可以了（其他形状同理）。</p></blockquote><p>构造方法：<br><code>BitmapShader(Bitmap bitmap, Shader.TileMode tileX, Shader.TileMode tileY)</code></p><p>参数：<br><code>bitmap</code>：用来做模板的 <code>Bitmap</code> 对象<br><code>tileX</code>：横向的 <code>TileMode</code><br><code>tileY</code>：纵向的 <code>TileMode</code>。</p><p><code>CLAMP</code>:</p><p><img src="images/52eb2279ly1fig6g2l2z3j20ks0chq4w.jpg" alt=""></p><p><code>MIRROR</code>:</p><p><img src="images/52eb2279ly1fig6g8moalj20kw0cjduk.jpg" alt=""></p><p><code>REPEAT</code>:</p><p><img src="images/52eb2279ly1fig6gcxlrfj20kv0ciduo.jpg" alt=""></p><h5 id="1-1-2-5-ComposeShader-混合着色器"><a href="#1-1-2-5-ComposeShader-混合着色器" class="headerlink" title="1.1.2.5 ComposeShader 混合着色器"></a>1.1.2.5 ComposeShader 混合着色器</h5><p>所谓混合，就是把两个 <code>Shader</code> 一起使用。</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">// 第一个 Shader：头像的 <span class="keyword">Bitmap</span></span><br><span class="line"><span class="keyword">Bitmap </span><span class="keyword">bitmap1 </span>= <span class="keyword">BitmapFactory.decodeResource(getResources(), </span>R.drawable.<span class="keyword">batman); </span> </span><br><span class="line"><span class="symbol">Shader</span> shader1 = new <span class="keyword">BitmapShader(bitmap1, </span>Shader.TileMode.CLAMP, Shader.TileMode.CLAMP)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">// 第二个 Shader：从上到下的线性渐变（由透明到黑色）</span><br><span class="line"><span class="keyword">Bitmap </span><span class="keyword">bitmap2 </span>= <span class="keyword">BitmapFactory.decodeResource(getResources(), </span>R.drawable.<span class="keyword">batman_logo); </span> </span><br><span class="line"><span class="symbol">Shader</span> shader2 = new <span class="keyword">BitmapShader(bitmap2, </span>Shader.TileMode.CLAMP, Shader.TileMode.CLAMP)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line">// ComposeShader：结合两个 Shader</span><br><span class="line"><span class="symbol">Shader</span> shader = new ComposeShader(shader1, shader2, PorterDuff.Mode.SRC_OVER)<span class="comment">;  </span></span><br><span class="line"><span class="symbol">paint.setShader</span>(shader)<span class="comment">;</span></span><br><span class="line"></span><br><span class="line"><span class="symbol">...</span></span><br><span class="line"></span><br><span class="line"><span class="symbol">canvas.drawCircle</span>(<span class="number">300</span>, <span class="number">300</span>, <span class="number">300</span>, paint)<span class="comment">;</span></span><br></pre></td></tr></table></figure><blockquote><p>注意：上面这段代码中我使用了两个 <code>BitmapShader</code> 来作为 <code>ComposeShader()</code> 的参数，而 <code>ComposeShader()</code> 在硬件加速下是不支持两个相同类型的 <code>Shader</code> 的，所以这里也需要关闭硬件加速才能看到效果。</p></blockquote><p><img src="images/52eb2279ly1fig6hbeg7gj20qy08cafn.jpg" alt=""></p><p>构造方法：<code>ComposeShader(Shader shaderA, Shader shaderB, PorterDuff.Mode mode)</code></p><p>参数：<br><code>shaderA</code>, <code>shaderB</code>：两个相继使用的 <code>Shader</code><br><code>mode</code>: 两个 <code>Shader</code> 的叠加模式，即 <code>shaderA</code> 和 <code>shaderB</code> 应该怎样共同绘制。它的类型是 <code>PorterDuff.Mode</code> 。</p><blockquote><p>PorterDuff.Mode</p><p><code>PorterDuff.Mode</code> 是用来指定两个图像共同绘制时的颜色策略的。它是一个 enum，不同的 <code>Mode</code> 可以指定不同的策略。「颜色策略」的意思，就是说把源图像绘制到目标图像处时应该怎样确定二者结合后的颜色，而对于 <code>ComposeShader(shaderA, shaderB, mode)</code> 这个具体的方法，就是指应该怎样把 <code>shaderB</code> 绘制在 <code>shaderA</code> 上来得到一个结合后的 <code>Shader</code>。</p><p>没有听说过 <code>PorterDuff.Mode</code> 的人，看到这里很可能依然会一头雾水：「什么怎么结合？就……两个图像一叠加，结合呗？还能怎么结合？」你还别说，还真的是有很多种策略来结合。</p><p>最符合直觉的结合策略，就是我在上面这个例子中使用的 <code>Mode</code>: <code>SRC_OVER</code>。它的算法非常直观：就像上面图中的那样，把源图像直接铺在目标图像上。不过，除了这种，其实还有一些其他的结合方式。例如如果我把上面例子中的参数 <code>mode</code> 改为 <code>PorterDuff.Mode.DST_OUT</code>，就会变成挖空效果：</p><p><img src="images/52eb2279ly1fig6hhwy65j20ql09ate9.jpg" alt=""></p><p>而如果再把 <code>mode</code> 改为 <code>PorterDuff.Mode.DST_IN</code>，就会变成蒙版抠图效果：</p><p><img src="images/52eb2279ly1fig6hxsdpij20qv09p43m.jpg" alt=""></p><p>这下明白了吧？</p><p>具体来说， <code>PorterDuff.Mode</code> 一共有 17 个，可以分为两类：</p><ol><li>Alpha 合成 (Alpha Compositing)</li><li>混合 (Blending)</li></ol><p>第一类，Alpha 合成，其实就是 「PorterDuff」 这个词所指代的算法。 「PorterDuff」 并不是一个具有实际意义的词组，而是两个人的名字（准确讲是姓）。这两个人当年共同发表了一篇论文，描述了 12 种将两个图像共同绘制的操作（即算法）。而这篇论文所论述的操作，都是关于 Alpha 通道（也就是我们通俗理解的「透明度」）的计算的，后来人们就把这类计算称为<strong>Alpha 合成</strong> ( Alpha Compositing ) 。</p><p>看下效果吧。效果直接盗 Google 的<a href="https://developer.android.com/reference/android/graphics/PorterDuff.Mode.html" target="_blank" rel="noopener">官方文档</a>了。</p><p>源图像和目标图像：</p><p><img src="images/52eb2279ly1fig6ia1twgj20ds07tdgs.jpg" alt=""></p><p>Alpha 合成：</p><p><img src="images/52eb2279ly1fig6im3hhcj20o50zt7bj.jpg" alt=""></p><p>第二类，混合，也就是 Photoshop 等制图软件里都有的那些混合模式（<code>multiply</code> <code>darken</code> <code>lighten</code>之类的）。这一类操作的是颜色本身而不是 <code>Alpha</code> 通道，并不属于 <code>Alpha</code> 合成，所以和 Porter 与 Duff 这两个人也没什么关系，不过为了使用的方便，它们同样也被 Google 加进了 <code>PorterDuff.Mode</code>里。</p><p>效果依然盗 <a href="https://developer.android.com/reference/android/graphics/PorterDuff.Mode.html" target="_blank" rel="noopener">官方文档</a>。</p><p><img src="images/52eb2279ly1fig6iw04v0j20ny0hzmzj.jpg" alt=""></p><h5 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h5><p>从效果图可以看出，Alpha 合成类的效果都比较直观，基本上可以使用简单的口头表达来描述它们的算法（起码对于不透明的源图像和目标图像来说是可以的），例如 <code>SRC_OVER</code> 表示「二者都绘制，但要源图像放在目标图像的上面」，<code>DST_IN</code> 表示「只绘制目标图像，并且只绘制它和源图像重合的区域」。</p><p>而混合类的效果就相对抽象一些，只从效果图不太能看得出它们的着色算法，更看不出来它们有什么用。不过没关系，你如果拿着这些名词去问你司的设计师，他们八成都能给你说出来个 123。</p><p>所以对于这些 <code>Mode</code>，正确的做法是：<strong>对于 Alpha 合成类的操作，掌握他们，并在实际开发中灵活运用；而对于混合类的，你只要把它们的名字记住就好了，这样当某一天设计师告诉你「我要做这种混合效果」的时候，你可以马上知道自己能不能做，怎么做。</strong></p><p>另外：<code>PorterDuff.Mode</code> 建议你动手用一下试试，对加深理解有帮助。</p></blockquote><p>好了，这些就是几个 <code>Shader</code> 的具体介绍。</p><p>除了使用 <code>setColor/ARGB()</code> 和 <code>setShader()</code> 来设置基本颜色， <code>Paint</code> 还可以来设置 <code>ColorFilter</code>，来对颜色进行第二层处理。</p><h3 id="1-2-setColorFilter-ColorFilter-colorFilter"><a href="#1-2-setColorFilter-ColorFilter-colorFilter" class="headerlink" title="1.2 setColorFilter(ColorFilter colorFilter)"></a>1.2 setColorFilter(ColorFilter colorFilter)</h3><p><code>ColorFilter</code> 这个类，它的名字已经足够解释它的作用：为绘制设置颜色过滤。颜色过滤的意思，就是为绘制的内容设置一个统一的过滤策略，然后 <code>Canvas.drawXXX()</code> 方法会对每个像素都进行过滤后再绘制出来。举几个现实中比较常见的颜色过滤的例子：</p><ul><li><p>有色光照射：</p><p>  <img src="images/52eb2279ly1fig6j51ronj20rs0kv1kx.jpg" alt="w400"></p></li><li><p>有色玻璃透视：</p><p>  <img src="images/52eb2279ly1fig6jizpvnj20iw0db4ey.jpg" alt="w400"></p></li><li><p>胶卷：</p><p>  <img src="images/52eb2279ly1fig6jv8idsj206404v40i.jpg" alt=""></p></li></ul><p>在 <code>Paint</code> 里设置 <code>ColorFilter</code> ，使用的是 <code>Paint.setColorFilter(ColorFilter filter)</code> 方法。 <code>ColorFilter</code> 并不直接使用，而是使用它的子类。它共有三个子类：<code>LightingColorFilter``PorterDuffColorFilter</code> 和 <code>ColorMatrixColorFilter</code>。</p><h4 id="1-2-1-LightingColorFilter"><a href="#1-2-1-LightingColorFilter" class="headerlink" title="1.2.1 LightingColorFilter"></a>1.2.1 LightingColorFilter</h4><p>这个 <code>LightingColorFilter</code> 是用来模拟简单的光照效果的。</p><p><code>LightingColorFilter</code> 的构造方法是 <code>LightingColorFilter(int mul, int add)</code> ，参数里的 <code>mul</code> 和 <code>add</code> 都是和颜色值格式相同的 int 值，其中 <code>mul</code> 用来和目标像素相乘，<code>add</code> 用来和目标像素相加：</p><figure class="highlight llvm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">R' = R * <span class="keyword">mul</span>.R / <span class="number">0xff</span> + <span class="keyword">add</span>.R  </span><br><span class="line">G' = G * <span class="keyword">mul</span>.G / <span class="number">0xff</span> + <span class="keyword">add</span>.G  </span><br><span class="line">B' = B * <span class="keyword">mul</span>.B / <span class="number">0xff</span> + <span class="keyword">add</span>.B</span><br></pre></td></tr></table></figure><p>一个「保持原样」的「基本 <code>LightingColorFilter</code> 」，<code>mul</code> 为 <code>0xffffff</code>，<code>add</code> 为 <code>0x000000</code>（也就是0），那么对于一个像素，它的计算过程就是：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">R' = R * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = R <span class="comment">// R' = R  </span></span><br><span class="line">G' = G * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = G <span class="comment">// G' = G  </span></span><br><span class="line">B' = B * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = B <span class="comment">// B' = B</span></span><br></pre></td></tr></table></figure><p>基于这个「基本 <code>LightingColorFilter</code> 」，你就可以修改一下做出其他的 filter。比如，如果你想去掉原像素中的红色，可以把它的 <code>mul</code> 改为 <code>0x00ffff</code> （红色部分为 0 ） ，那么它的计算过程就是：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">R' = R * <span class="number">0x0</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = <span class="number">0</span> <span class="comment">// 红色被移除  </span></span><br><span class="line">G' = G * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = G  </span><br><span class="line">B' = B * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = B</span><br></pre></td></tr></table></figure><p>具体效果是这样的：</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ColorFilter</span> lightingColorFilter = <span class="function"><span class="keyword">new</span> <span class="title">LightingColorFilter</span>(<span class="number">0x00ffff</span>, <span class="number">0x000000</span>);  </span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setColorFilter</span>(lightingColorFilter);</span></span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6k1n8aij209104faay.jpg" alt=""></p><blockquote><p>表情忽然变得阴郁了</p></blockquote><p>或者，如果你想让它的绿色更亮一些，就可以把它的 <code>add</code> 改为 <code>0x003000</code> （绿色部分为 0x30 ），那么它的计算过程就是：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">R' = R * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = R  </span><br><span class="line">G' = G * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x30</span> = G + <span class="number">0x30</span> <span class="comment">// 绿色被加强  </span></span><br><span class="line">B' = B * <span class="number">0xff</span> / <span class="number">0xff</span> + <span class="number">0x0</span> = B</span><br></pre></td></tr></table></figure><p>效果是这样：</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">ColorFilter</span> lightingColorFilter = <span class="function"><span class="keyword">new</span> <span class="title">LightingColorFilter</span>(<span class="number">0xffffff</span>, <span class="number">0x003000</span>);  </span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setColorFilter</span>(lightingColorFilter);</span></span><br></pre></td></tr></table></figure><p><img src="images/52eb2279ly1fig6k91tiyj209f04dmy3.jpg" alt=""></p><blockquote><p>这样的表情才阳光</p></blockquote><p>至于怎么修改参数来模拟你想要的某种具体光照效果，你就别问我了，还是跟你司设计师讨论吧，这个我不专业……</p><h4 id="1-2-2-PorterDuffColorFilter"><a href="#1-2-2-PorterDuffColorFilter" class="headerlink" title="1.2.2 PorterDuffColorFilter"></a>1.2.2 PorterDuffColorFilter</h4><p>这个 <code>PorterDuffColorFilter</code> 的作用是使用一个指定的颜色和一种指定的 <code>PorterDuff.Mode</code> 来与绘制对象进行合成。它的构造方法是 <code>PorterDuffColorFilter(int color, PorterDuff.Mode mode)</code> 其中的 <code>color</code> 参数是指定的颜色， <code>mode</code> 参数是指定的 <code>Mode</code>。同样也是 <code>PorterDuff.Mode</code> ，不过和 <code>ComposeShader</code> 不同的是，<code>PorterDuffColorFilter</code> 作为一个 <code>ColorFilter</code>，只能指定一种颜色作为源，而不是一个 <code>Bitmap</code>。</p><p><code>PorterDuff.Mode</code> 前面已经讲过了，而 <code>PorterDuffColorFilter</code> 本身的使用是非常简单的，所以不再展开讲。</p><h4 id="1-2-3-ColorMatrixColorFilter"><a href="#1-2-3-ColorMatrixColorFilter" class="headerlink" title="1.2.3 ColorMatrixColorFilter"></a>1.2.3 ColorMatrixColorFilter</h4><p>这个就厉害了。<code>ColorMatrixColorFilter</code> 使用一个 <code>ColorMatrix</code> 来对颜色进行处理。 <code>ColorMatrix</code>这个类，内部是一个 4x5 的矩阵：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">[ a, b, c, d, e,</span><br><span class="line">  f, g, h, i, j,</span><br><span class="line">  k, l, m, n, o,</span><br><span class="line">  p, q, r, s, t ]</span><br></pre></td></tr></table></figure><p>通过计算， <code>ColorMatrix</code> 可以把要绘制的像素进行转换。对于颜色 [R, G, B, A] ，转换算法是这样的：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">R’ = a*R + <span class="keyword">b*G </span>+ c*<span class="keyword">B </span>+ d*A + e<span class="comment">;  </span></span><br><span class="line">G’ = f*R + g*G + h*<span class="keyword">B </span>+ i*A + <span class="keyword">j; </span> </span><br><span class="line"><span class="keyword">B’ </span>= k*R + l*G + m*<span class="keyword">B </span>+ n*A + o<span class="comment">;  </span></span><br><span class="line">A’ = p*R + q*G + r*<span class="keyword">B </span>+ s*A + t<span class="comment">;</span></span><br></pre></td></tr></table></figure><p><code>ColorMatrix</code> 有一些自带的方法可以做简单的转换，例如可以使用 <code>setSaturation(float sat)</code> 来设置饱和度；另外你也可以自己去设置它的每一个元素来对转换效果做精细调整。具体怎样设置会有怎样的效果，我就不讲了（其实是我也不太会😅）。如果你有需求，可以试一下程大治同学做的这个库：<a href="https://github.com/chengdazhi/StyleImageView" target="_blank" rel="noopener">StyleImageView</a></p><p><img src="images/006tNc79ly1fig6vhi48zg309c0fzqv7.gif" alt=""></p><p>以上，就是 <code>Paint</code> 对颜色的第二层处理：通过 <code>setColorFilter(colorFilter)</code> 来加工颜色。</p><p>除了基本颜色的设置（ <code>setColor/ARGB()</code>, <code>setShader()</code> ）以及基于原始颜色的过滤（ <code>setColorFilter()</code> ）之外，<code>Paint</code> 最后一层处理颜色的方法是 <code>setXfermode(Xfermode xfermode)</code> ，它处理的是「当颜色遇上 View」的问题。</p><h3 id="1-3-setXfermode-Xfermode-xfermode"><a href="#1-3-setXfermode-Xfermode-xfermode" class="headerlink" title="1.3 setXfermode(Xfermode xfermode)"></a>1.3 setXfermode(Xfermode xfermode)</h3><p>“Xfermode” 其实就是 “Transfer mode”，用 “X” 来代替 “Trans” 是一些美国人喜欢用的简写方式。严谨地讲， <code>Xfermode</code> 指的是你要绘制的内容和 <code>Canvas</code> 的目标位置的内容应该怎样结合计算出最终的颜色。但通俗地说，其实就是要你以绘制的内容作为源图像，以 View 中已有的内容作为目标图像，选取一个 <code>PorterDuff.Mode</code> 作为绘制内容的颜色处理方案。就像这样：</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Xfermode</span> xfermode = <span class="function"><span class="keyword">new</span> <span class="title">PorterDuffXfermode</span>(<span class="type">PorterDuff</span>.<span class="type">Mode</span>.<span class="type">DST_IN</span>);</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">...</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawBitmap</span>(rectBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画方  </span></span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setXfermode</span>(xfermode); <span class="comment">// 设置 Xfermode  </span></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawBitmap</span>(circleBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画圆  </span></span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setXfermode</span>(null); <span class="comment">// 用完及时清除 Xfermode</span></span></span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig6x8dhk6j30nl0pswff.jpg" alt=""></p><p><img src="images/006tNc79ly1fig70910n6g30eg0b4x6p.gif" alt=""></p><p>又是 <code>PorterDuff.Mode</code> 。 <code>PorterDuff.Mode</code> 在 <code>Paint</code> 一共有三处 API ，它们的工作原理都一样，只是用途不同：</p><p><img src="images/006tNc79ly1fig70nh3gpj30fs04uwez.jpg" alt=""></p><p>另外，从上面的示例代码可以看出，创建 <code>Xfermode</code> 的时候其实是创建的它的子类 <code>PorterDuffXfermode</code>。而事实上，<code>Xfermode</code> 也只有这一个子类。所以在设置 <code>Xfermode</code> 的时候不用多想，直接用 <code>PorterDuffXfermode</code> 吧。</p><blockquote><p>「只有一个子类？？？什么设计？」</p><p><img src="images/006tNc79ly1fig71bvcguj305t05wq35.jpg" alt=""></p><p>其实在更早的 Android 版本中，<code>Xfermode</code> 还有别的子类，但别的子类现在已经 <code>deprecated</code> 了，如今只剩下了 <code>PorterDuffXfermode</code>。所以目前它的使用看起来好像有点啰嗦，但其实是由于历史遗留问题。</p></blockquote><h4 id="Xfermode-注意事项"><a href="#Xfermode-注意事项" class="headerlink" title="Xfermode 注意事项"></a>Xfermode 注意事项</h4><p><code>Xfermode</code> 使用很简单，不过有两点需要注意：</p><h5 id="1-使用离屏缓冲（Off-screen-Buffer）"><a href="#1-使用离屏缓冲（Off-screen-Buffer）" class="headerlink" title="1. 使用离屏缓冲（Off-screen Buffer）"></a>1. 使用离屏缓冲（Off-screen Buffer）</h5><p>实质上，上面这段例子代码，如果直接执行的话是不会绘制出图中效果的，程序的绘制也不会像上面的动画那样执行，而是会像这样：</p><p><img src="images/006tNc79ly1fig71o7qskj30nl0pl0tx.jpg" alt=""></p><blockquote><p>为什么会这样？</p></blockquote><p>按照逻辑我们会认为，在第二步画圆的时候，跟它共同计算的是第一步绘制的方形。但实际上，却是整个 <code>View</code> 的显示区域都在画圆的时候参与计算，并且 <code>View</code> 自身的底色并不是默认的透明色，而且是遵循一种迷之逻辑，导致不仅绘制的是整个圆的范围，而且在范围之外都变成了黑色。就像这样：</p><p><img src="images/006tNc79ly1fig721mjemg30eg0b4u0x.gif" alt=""></p><blockquote><p>这……那可如何是好？</p></blockquote><p>要想使用 <code>setXfermode()</code> 正常绘制，必须使用离屏缓存 (Off-screen Buffer) 把内容绘制在额外的层上，再把绘制好的内容贴回 View 中。也就是这样：</p><p><img src="images/006tNc79ly1fig72kvygag30eg0b4npd.gif" alt=""></p><p>通过使用离屏缓冲，把要绘制的内容单独绘制在缓冲层， <code>Xfermode</code> 的使用就不会出现奇怪的结果了。使用离屏缓冲有两种方式：</p><ul><li><p><code>Canvas.saveLayer()</code></p><p>  <code>saveLayer()</code> 可以做短时的离屏缓冲。使用方法很简单，在绘制代码的前后各加一行代码，在绘制之前保存，绘制之后恢复：</p>  <figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">int <span class="built_in">saved</span> = canvas.saveLayer(<span class="built_in">null</span>, <span class="built_in">null</span>, Canvas.ALL_SAVE_FLAG);</span><br><span class="line"></span><br><span class="line">canvas.drawBitmap(rectBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画方</span></span><br><span class="line">paint.setXfermode(xfermode); <span class="comment">// 设置 Xfermode</span></span><br><span class="line">canvas.drawBitmap(circleBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画圆</span></span><br><span class="line">paint.setXfermode(<span class="built_in">null</span>); <span class="comment">// 用完及时清除 Xfermode</span></span><br><span class="line"></span><br><span class="line">canvas.restoreToCount(<span class="built_in">saved</span>);</span><br></pre></td></tr></table></figure></li><li><p><code>View.setLayerType()</code></p><p>  <code>View.setLayerType()</code> 是直接把整个 <code>View</code> 都绘制在离屏缓冲中。 <code>setLayerType(LAYER_TYPE_HARDWARE)</code> 是使用 GPU 来缓冲， <code>setLayerType(LAYER_TYPE_SOFTWARE)</code> 是直接直接用一个 <code>Bitmap</code> 来缓冲。</p></li></ul><blockquote><p>关于 <code>Canvas.saveLayer()</code> 和 <code>View.setLayerType()</code> ，这里就不细讲它们的意义和原理了，后面也许我会专门用一期来讲它们。</p></blockquote><p>如果没有特殊需求，可以选用第一种方法 <code>Canvas.saveLayer()</code> 来设置离屏缓冲，以此来获得更高的性能。更多关于离屏缓冲的信息，可以看<a href="https://developer.android.com/guide/topics/graphics/hardware-accel.html" target="_blank" rel="noopener">官方文档</a>中对于硬件加速的介绍。</p><h5 id="2-控制好透明区域"><a href="#2-控制好透明区域" class="headerlink" title="2. 控制好透明区域"></a>2. 控制好透明区域</h5><p>使用 Xfermode 来绘制的内容，除了注意使用离屏缓冲，还应该注意控制它的透明区域不要太小，要让它足够覆盖到要和它结合绘制的内容，否则得到的结果很可能不是你想要的。我用图片来具体说明一下：</p><p><img src="images/006tNc79ly1fig73037soj30sj0x3myt.jpg" alt=""></p><blockquote><p>如图所示，由于透明区域过小而覆盖不到的地方，将不会受到 Xfermode 的影响。</p></blockquote><p>好，到此为止，前面讲的就是 <code>Paint</code> 的第一类 API——关于颜色的三层设置：直接设置颜色的 API 用来给图形和文字设置颜色； <code>setColorFilter()</code> 用来基于颜色进行过滤处理； <code>setXfermode()</code> 用来处理源图像和 <code>View</code> 已有内容的关系。</p><p>再贴一次本章开始处的图作为回顾：</p><p><img src="images/006tNc79ly1fig738su5oj30j909ymy2.jpg" alt=""></p><h2 id="2-效果"><a href="#2-效果" class="headerlink" title="2 效果"></a>2 效果</h2><p>效果类的 API ，指的就是抗锯齿、填充/轮廓、线条宽度等等这些。</p><h3 id="2-1-setAntiAlias-boolean-aa-设置抗锯齿"><a href="#2-1-setAntiAlias-boolean-aa-设置抗锯齿" class="headerlink" title="2.1 setAntiAlias (boolean aa) 设置抗锯齿"></a>2.1 setAntiAlias (boolean aa) 设置抗锯齿</h3><p>抗锯齿在上一节已经讲过了，话不多说，直接上图：</p><p><img src="images/006tNc79ly1fig73fpp5aj30ks0bqt9f.jpg" alt=""></p><p>抗锯齿默认是关闭的，如果需要抗锯齿，需要显式地打开。另外，除了 <code>setAntiAlias(aa)</code> 方法，打开抗锯齿还有一个更方便的方式：构造方法。创建 <code>Paint</code> 对象的时候，构造方法的参数里加一个 <code>ANTI_ALIAS_FLAG</code> 的 flag，就可以在初始化的时候就开启抗锯齿。</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Paint paint</span> = new Paint(Paint.ANTI_ALIAS_FLAG);</span><br></pre></td></tr></table></figure><h3 id="2-2-setStyle-Paint-Style-style"><a href="#2-2-setStyle-Paint-Style-style" class="headerlink" title="2.2 setStyle(Paint.Style style)"></a>2.2 setStyle(Paint.Style style)</h3><p><code>setStyle(style)</code> 也在上一节讲过了，用来设置图形是线条风格还是填充风格的（也可以二者并用）：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.FILL); <span class="comment">// FILL 模式，填充  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig73rw5yoj303w03s0sm.jpg" alt=""></p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.STROKE); <span class="comment">// STROKE 模式，画线  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7lnxj6oj303z03ta9y.jpg" alt=""></p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.FILL_AND_STROKE); <span class="comment">// FILL_AND_STROKE 模式，填充 + 画线  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7litstsj303w03s0sm.jpg" alt=""></p><p><code>FILL</code> 模式是默认模式，所以如果之前没有设置过其他的 <code>Style</code>，可以不用 <code>setStyle(Paint.Style.FILL)</code> 这句。</p><h3 id="2-3-线条形状"><a href="#2-3-线条形状" class="headerlink" title="2.3 线条形状"></a>2.3 线条形状</h3><p>设置线条形状的一共有 4 个方法：<code>setStrokeWidth(float width)</code>, <code>setStrokeCap(Paint.Cap cap)</code>, <code>setStrokeJoin(Paint.Join join)</code>, <code>setStrokeMiter(float miter)</code> 。</p><h4 id="2-3-1-setStrokeWidth-float-width"><a href="#2-3-1-setStrokeWidth-float-width" class="headerlink" title="2.3.1 setStrokeWidth(float width)"></a>2.3.1 setStrokeWidth(float width)</h4><p>设置线条宽度。单位为像素，默认值是 0。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.STROKE);  </span><br><span class="line">paint.setStrokeWidth(<span class="number">1</span>);  </span><br><span class="line">canvas.drawCircle(<span class="number">150</span>, <span class="number">125</span>, <span class="number">100</span>, paint);  </span><br><span class="line">paint.setStrokeWidth(<span class="number">5</span>);  </span><br><span class="line">canvas.drawCircle(<span class="number">400</span>, <span class="number">125</span>, <span class="number">100</span>, paint);  </span><br><span class="line">paint.setStrokeWidth(<span class="number">40</span>);  </span><br><span class="line">canvas.drawCircle(<span class="number">650</span>, <span class="number">125</span>, <span class="number">100</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7407tp7j30lj07ljrr.jpg" alt=""></p><blockquote><p>线条宽度 0 和 1 的区别</p><p>默认情况下，线条宽度为 0，但你会发现，这个时候它依然能够画出线，线条的宽度为 1 像素。那么它和线条宽度为 1 有什么区别呢？</p><p>其实这个和后面要讲的一个「几何变换」有关：你可以为 <code>Canvas</code> 设置 <code>Matrix</code> 来实现几何变换（如放大、缩小、平移、旋转），在几何变换之后 <code>Canvas</code> 绘制的内容就会发生相应变化，包括线条也会加粗，例如 2 像素宽度的线条在 <code>Canvas</code> 放大 2 倍后会被以 4 像素宽度来绘制。而当线条宽度被设置为 0 时，它的宽度就被固定为 1 像素，就算 <code>Canvas</code> 通过几何变换被放大，它也依然会被以 1 像素宽度来绘制。Google 在文档中把线条宽度为 0 时称作「hairline mode（发际线模式）」。</p></blockquote><h4 id="2-3-2-setStrokeCap-Paint-Cap-cap"><a href="#2-3-2-setStrokeCap-Paint-Cap-cap" class="headerlink" title="2.3.2 setStrokeCap(Paint.Cap cap)"></a>2.3.2 setStrokeCap(Paint.Cap cap)</h4><p>设置线头的形状。线头形状有三种：<code>BUTT</code> 平头、<code>ROUND</code> 圆头、<code>SQUARE</code> 方头。默认为 <code>BUTT</code>。</p><blockquote><p>放出「平头」「圆头」「方头」这种翻译我始终有点纠结：既觉得自己翻译得简洁清晰尽显机智，同时又担心用词会不会有点太过通俗，让人觉得我不够高贵冷艳？</p></blockquote><p>当线条的宽度是 1 像素时，这三种线头的表现是完全一致的，全是 1 个像素的点；而当线条变粗的时候，它们就会表现出不同的样子：</p><p><img src="images/006tNc79ly1fig74qv8rij30ct05rglp.jpg" alt=""></p><p>虚线是额外加的，虚线左边是线的实际长度，虚线右边是线头。有了虚线作为辅助，可以清楚地看出 <code>BUTT</code> 和 <code>SQUARE</code> 的区别。</p><h4 id="2-3-3-setStrokeJoin-Paint-Join-join"><a href="#2-3-3-setStrokeJoin-Paint-Join-join" class="headerlink" title="2.3.3 setStrokeJoin(Paint.Join join)"></a>2.3.3 setStrokeJoin(Paint.Join join)</h4><p>设置拐角的形状。有三个值可以选择：<code>MITER</code> 尖角、 <code>BEVEL</code> 平角和 <code>ROUND</code> 圆角。默认为 <code>MITER</code>。</p><p><img src="images/006tNc79ly1fig75e27w6j30cp05ewem.jpg" alt=""></p><blockquote><p>辅助理解：</p><p>MITER 在现实中其实就是这玩意：</p><p><img src="images/006tNc79ly1fig75ne2avj30jw0avt9n.jpg" alt=""></p><p>而 BEVEL 是这玩意：</p><p><img src="images/006tNc79ly1fig762ptvdj307u05l74d.jpg" alt=""></p></blockquote><h4 id="2-3-4-setStrokeMiter-float-miter"><a href="#2-3-4-setStrokeMiter-float-miter" class="headerlink" title="2.3.4 setStrokeMiter(float miter)"></a>2.3.4 setStrokeMiter(float miter)</h4><p>这个方法是对于 <code>setStrokeJoin()</code> 的一个补充，它用于设置 <code>MITER</code> 型拐角的延长线的最大值。所谓「延长线的最大值」，是这么一回事：</p><p>当线条拐角为 <code>MITER</code> 时，拐角处的外缘需要使用延长线来补偿：</p><p><img src="images/006tNc79ly1fig7ak3kqgj30cs0cq74j.jpg" alt=""></p><p>而这种补偿方案会有一个问题：如果拐角的角度太小，就有可能由于出现连接点过长的情况。比如这样：</p><p><img src="images/006tNc79ly1fig7bdt8wxj30eg0673yl.jpg" alt=""></p><p>所以为了避免意料之外的过长的尖角出现， <code>MITER</code> 型连接点有一个额外的规则：当尖角过长时，自动改用 <code>BEVEL</code> 的方式来渲染连接点。例如上图的这个尖角，在默认情况下是不会出现的，而是会由于延长线过长而被转为 <code>BEVEL</code> 型连接点：</p><p><img src="images/006tNc79ly1fig7bonurpj307502yjra.jpg" alt=""></p><p>至于多尖的角属于过于尖，尖到需要转为使用 <code>BEVEL</code> 来绘制，则是由一个属性控制的，而这个属性就是 <code>setStrokeMiter(miter)</code> 方法中的 <code>miter</code> 参数。<code>miter</code> 参数是对于转角长度的限制，具体来讲，是指尖角的外缘端点和内部拐角的距离与线条宽度的比。也就是下面这两个长度的比：</p><p><img src="images/006tNc79ly1fig7btolhij30e706dglp.jpg" alt=""></p><p>用几何知识很容易得出这个比值的计算公式：如果拐角的大小为 θ ，那么这个比值就等于 1 / sin ( θ / 2 ) 。</p><p>这个 miter limit 的默认值是 4，对应的是一个大约 29° 的锐角：</p><p><img src="images/006tNc79ly1fig7bytfd9j307n03lq2v.jpg" alt=""></p><blockquote><p>默认情况下，大于这个角的尖角会被保留，而小于这个夹角的就会被「削成平头」</p><p>所以，这个方法虽然名叫 <code>setStrokeMiter(miter)</code> ，但它其实设置的是「 线条在 <code>Join</code> 类型为 <code>MITER</code> 时对于 <code>MITER</code> 的长度限制」。它的这个名字虽然短，但却存在一定的迷惑性，如果叫 <code>setStrokeJoinMiterLimit(limit)</code> 就更准确了。 Google 的工程师没有这么给它命名，大概也是不想伤害大家的手指吧，毕竟程序员何苦为难程序员。</p><p><img src="images/006tNc79ly1fig7c4wf8uj304z05et8u.jpg" alt=""></p></blockquote><p>以上就是 4 个关于线条形状的方法： <code>setStrokeWidth(width)</code> <code>setStrokeCap(cap)``setStrokeJoint(join)</code> 和 <code>setStrokeMiter(miter)</code>。</p><h3 id="2-4-色彩优化"><a href="#2-4-色彩优化" class="headerlink" title="2.4 色彩优化"></a>2.4 色彩优化</h3><p><code>Paint</code> 的色彩优化有两个方法： <code>setDither(boolean dither)</code> 和 <code>setFilterBitmap(boolean filter)</code>。它们的作用都是让画面颜色变得更加「顺眼」，但原理和使用场景是不同的。</p><h4 id="2-4-1-setDither-boolean-dither"><a href="#2-4-1-setDither-boolean-dither" class="headerlink" title="2.4.1 setDither(boolean dither)"></a>2.4.1 setDither(boolean dither)</h4><p>设置图像的抖动。</p><p>在介绍抖动之前，先来看一个猥琐男：</p><p><img src="images/006tNc79ly1fig7cf7oc3j30i30a4wfi.jpg" alt=""></p><p>注意毛利小五郎脸上的红晕，它们并不是使用一片淡红色涂抹出来的，而是画了三道深色的红线。这三道深色红线放在脸上，给人的视觉效果就成了「淡淡的红晕」。</p><p>抖动的原理和这个类似。所谓抖动（注意，它就叫抖动，不是防抖动，也不是去抖动，有些人在翻译的时候自作主张地加了一个「防」字或者「去」字，这是不对的），是指把图像从较高色彩深度（即可用的颜色数）向较低色彩深度的区域绘制时，在图像中有意地插入噪点，通过有规律地扰乱图像来让图像对于肉眼更加真实的做法。</p><p>比如向 1 位色彩深度的区域中绘制灰色，由于 1 位深度只包含黑和白两种颜色，在默认情况下，即不加抖动的时候，只能选择向上或向下选择最接近灰色的白色或黑色来绘制，那么显示出来也只能是一片白或者一片黑。而加了抖动后，就可以绘制出让肉眼识别为灰色的效果了：</p><p><img src="images/006tNc79ly1fig7clzhfpj30k60armxr.jpg" alt=""></p><p>瞧，像上面这样，用黑白相间的方式来绘制，就可以骗过肉眼，让肉眼辨别为灰色了。</p><p>嗯？你说你看不出灰色，只看出黑白相间？没关系，那是因为像素颗粒太大，我把像素颗粒缩小，看到完整效果你就会发现变灰了：</p><p><img src="images/006tNc79ly1fig7ctjqsqj309q0bytan.jpg" alt=""></p><p>这下变灰了吧？</p><p>什么，还没有变灰？那一定是你看图的姿势不对了。</p><p><img src="images/006tNc79ly1fig7cxlyluj30gg0n00vf.jpg" alt=""></p><p>不过，抖动可不只可以用在纯色的绘制。在实际的应用场景中，抖动更多的作用是在图像降低色彩深度绘制时，避免出现大片的色带与色块。效果盗一下<a href="https://en.wikipedia.org/wiki/Dither" target="_blank" rel="noopener">维基百科</a>的图：</p><p><img src="images/006tNc79ly1fig7d34s0jj30lf07t75x.jpg" alt=""></p><p>看着很牛逼对吧？确实很牛逼，而且在 Android 里使用起来也很简单，一行代码就搞定：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">paint.setDither(true)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p>只要加这么一行代码，之后的绘制就是加抖动的了。</p><p>不过对于现在（2017年）而言， <code>setDither(dither)</code> 已经没有当年那么实用了，因为现在的 Android 版本的绘制，默认的色彩深度已经是 32 位的 <code>ARGB_8888</code> ，效果已经足够清晰了。只有当你向自建的 <code>Bitmap</code> 中绘制，并且选择 16 位色的 <code>ARGB_4444</code> 或者 <code>RGB_565</code> 的时候，开启它才会有比较明显的效果。</p><h4 id="2-4-2-setFilterBitmap-boolean-filter"><a href="#2-4-2-setFilterBitmap-boolean-filter" class="headerlink" title="2.4.2 setFilterBitmap(boolean filter)"></a>2.4.2 setFilterBitmap(boolean filter)</h4><p>设置是否使用双线性过滤来绘制 <code>Bitmap</code> 。</p><p>图像在放大绘制的时候，默认使用的是最近邻插值过滤，这种算法简单，但会出现马赛克现象；而如果开启了双线性过滤，就可以让结果图像显得更加平滑。效果依然盗<a href="https://zh.wikipedia.org/wiki/%E5%8F%8C%E7%BA%BF%E6%80%A7%E8%BF%87%E6%BB%A4" target="_blank" rel="noopener">维基百科</a>的图：</p><p><img src="images/006tNc79ly1fig7dbga6ij30jb0a00tr.jpg" alt=""></p><p>牛逼吧？而且它的使用同样也很简单：</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Xfermode</span> xfermode = <span class="function"><span class="keyword">new</span> <span class="title">PorterDuffXfermode</span>(<span class="type">PorterDuff</span>.<span class="type">Mode</span>.<span class="type">DST_IN</span>);</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">...</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawBitmap</span>(rectBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画方  </span></span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setXfermode</span>(xfermode); <span class="comment">// 设置 Xfermode  </span></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawBitmap</span>(circleBitmap, <span class="number">0</span>, <span class="number">0</span>, paint); <span class="comment">// 画圆  </span></span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setXfermode</span>(null); <span class="comment">// 用完及时清除 Xfermode</span></span></span><br></pre></td></tr></table></figure><p>加上这一行，在放大绘制 <code>Bitmap</code> 的时候就会使用双线性过滤了。</p><p>以上就是 <code>Paint</code> 的两个色彩优化的方法： <code>setDither(dither)</code> ，设置抖动来优化色彩深度降低时的绘制效果； <code>setFilterBitmap(filterBitmap)</code> ，设置双线性过滤来优化 <code>Bitmap</code> 放大绘制的效果。</p><h3 id="2-5-setPathEffect-PathEffect-effect"><a href="#2-5-setPathEffect-PathEffect-effect" class="headerlink" title="2.5 setPathEffect(PathEffect effect)"></a>2.5 setPathEffect(PathEffect effect)</h3><p>使用 <code>PathEffect</code> 来给图形的轮廓设置效果。对 <code>Canvas</code> 所有的图形绘制有效，也就是 <code>drawLine()``drawCircle()</code> <code>drawPath()</code> 这些方法。大概像这样：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">PathEffect pathEffect = new DashPathEffect(new <span class="type">float</span>[]&#123;<span class="number">10</span>, <span class="number">5</span>&#125;, <span class="number">10</span>);  </span><br><span class="line">paint.setPathEffect(pathEffect);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7diyn5jj308h089mx5.jpg" alt=""></p><p>下面就具体说一下 Android 中的 6 种 <code>PathEffect</code>。<code>PathEffect</code> 分为两类，单一效果的 <code>CornerPathEffect</code> <code>DiscretePathEffect</code> <code>DashPathEffect</code> <code>PathDashPathEffect</code> ，和组合效果的 <code>SumPathEffect</code> <code>ComposePathEffect</code>。</p><h4 id="2-5-1-CornerPathEffect"><a href="#2-5-1-CornerPathEffect" class="headerlink" title="2.5.1 CornerPathEffect"></a>2.5.1 CornerPathEffect</h4><p>把所有拐角变成圆角。</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">PathEffect</span> pathEffect = <span class="function"><span class="keyword">new</span> <span class="title">CornerPathEffect</span>(<span class="number">20</span>);  </span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setPathEffect</span>(pathEffect);</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">...</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawPath</span>(path, paint);</span></span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7dobrizj30iv0agt8z.jpg" alt=""></p><p>它的构造方法 <code>CornerPathEffect(float radius)</code> 的参数 <code>radius</code> 是圆角的半径。</p><h4 id="2-5-2-DiscretePathEffect"><a href="#2-5-2-DiscretePathEffect" class="headerlink" title="2.5.2 DiscretePathEffect"></a>2.5.2 DiscretePathEffect</h4><p>把线条进行随机的偏离，让轮廓变得乱七八糟。乱七八糟的方式和程度由参数决定。</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">PathEffect</span> pathEffect = <span class="function"><span class="keyword">new</span> <span class="title">CornerPathEffect</span>(<span class="number">20</span>);  </span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setPathEffect</span>(pathEffect);</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">...</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawPath</span>(path, paint);</span></span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7dug01cj30ir0bawet.jpg" alt=""></p><p><code>DiscretePathEffect</code> 具体的做法是，把绘制改为使用定长的线段来拼接，并且在拼接的时候对路径进行随机偏离。它的构造方法 <code>DiscretePathEffect(float segmentLength, float deviation)</code> 的两个参数中， <code>segmentLength</code> 是用来拼接的每个线段的长度， <code>deviation</code> 是偏离量。这两个值设置得不一样，显示效果也会不一样，具体的你自己多试几次就明白了，这里不再贴更多的图。</p><h4 id="2-5-3-DashPathEffect"><a href="#2-5-3-DashPathEffect" class="headerlink" title="2.5.3 DashPathEffect"></a>2.5.3 DashPathEffect</h4><p>使用虚线来绘制线条。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">PathEffect pathEffect = new DashPathEffect(new <span class="type">float</span>[]&#123;<span class="number">20</span>, <span class="number">10</span>, <span class="number">5</span>, <span class="number">10</span>&#125;, <span class="number">0</span>);  </span><br><span class="line">paint.setPathEffect(pathEffect);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawPath(path, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7dz1jenj30iw0b9mxh.jpg" alt=""></p><p>它的构造方法 <code>DashPathEffect(float[] intervals, float phase)</code> 中， 第一个参数 <code>intervals</code> 是一个数组，它指定了虚线的格式：数组中元素必须为偶数（最少是 2 个），按照「画线长度、空白长度、画线长度、空白长度」……的顺序排列，例如上面代码中的 <code>20, 5, 10, 5</code> 就表示虚线是按照「画 20 像素、空 5 像素、画 10 像素、空 5 像素」的模式来绘制；第二个参数 <code>phase</code> 是虚线的偏移量。</p><h4 id="2-5-4-PathDashPathEffect"><a href="#2-5-4-PathDashPathEffect" class="headerlink" title="2.5.4 PathDashPathEffect"></a>2.5.4 PathDashPathEffect</h4><p>这个方法比 <code>DashPathEffect</code> 多一个前缀 <code>Path</code> ，所以顾名思义，它是使用一个 <code>Path</code> 来绘制「虚线」。具体看图吧：</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">Path</span> dashPath = ...; <span class="comment">// 使用一个三角形来做 dash  </span></span><br><span class="line"><span class="type">PathEffect</span> pathEffect = <span class="function"><span class="keyword">new</span> <span class="title">PathDashPathEffect</span>(dashPath, <span class="number">40</span>, <span class="number">0</span>,  </span></span><br><span class="line"><span class="function">        <span class="type">PathDashPathEffectStyle</span>.<span class="type">TRANSLATE</span>);</span></span><br><span class="line"><span class="function"><span class="title">paint</span>.<span class="title">setPathEffect</span>(pathEffect);</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">...</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">canvas</span>.<span class="title">drawPath</span>(path, paint);</span></span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7e4xp38j30j30bc0t7.jpg" alt=""></p><p>它的构造方法 <code>PathDashPathEffect(Path shape, float advance, float phase, PathDashPathEffect.Style style)</code>中， <code>shape</code> 参数是用来绘制的 <code>Path</code> ； <code>advance</code> 是两个相邻的 <code>shape</code> 段之间的间隔，不过注意，这个间隔是两个 <code>shape</code> 段的起点的间隔，而不是前一个的终点和后一个的起点的距离； <code>phase</code> 和 <code>DashPathEffect</code> 中一样，是虚线的偏移；最后一个参数 <code>style</code>，是用来指定拐弯改变的时候 <code>shape</code> 的转换方式。<code>style</code> 的类型为 <code>PathDashPathEffect.Style</code> ，是一个 <code>enum</code> ，具体有三个值：</p><ul><li><code>TRANSLATE</code>：位移</li><li><code>ROTATE</code>：旋转</li><li><code>MORPH</code>：变体</li></ul><p><img src="images/006tNc79ly1fig7efqw9qj30kn0h3dh5.jpg" alt=""></p><h4 id="2-5-5-SumPathEffect"><a href="#2-5-5-SumPathEffect" class="headerlink" title="2.5.5 SumPathEffect"></a>2.5.5 SumPathEffect</h4><p>这是一个组合效果类的 <code>PathEffect</code> 。它的行为特别简单，就是分别按照两种 <code>PathEffect</code> 分别对目标进行绘制。</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PathEffect dashEffect = <span class="keyword">new</span> <span class="type">DashPathEffect</span>(<span class="keyword">new</span> <span class="type">float</span>[]&#123;<span class="number">20</span>, <span class="number">10</span>&#125;, <span class="number">0</span>);  </span><br><span class="line">PathEffect discreteEffect = <span class="keyword">new</span> <span class="type">DiscretePathEffect</span>(<span class="number">20</span>, <span class="number">5</span>);  </span><br><span class="line">pathEffect = <span class="keyword">new</span> <span class="type">SumPathEffect</span>(dashEffect, discreteEffect);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawPath(path, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7ekjh7lj30dw05jq2z.jpg" alt=""></p><h4 id="2-5-6-ComposePathEffect"><a href="#2-5-6-ComposePathEffect" class="headerlink" title="2.5.6 ComposePathEffect"></a>2.5.6 ComposePathEffect</h4><p>这也是一个组合效果类的 <code>PathEffect</code> 。不过它是先对目标 <code>Path</code> 使用一个 <code>PathEffect</code>，然后再对这个改变后的 <code>Path</code> 使用另一个 <code>PathEffect</code>。</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">PathEffect dashEffect = <span class="keyword">new</span> <span class="type">DashPathEffect</span>(<span class="keyword">new</span> <span class="type">float</span>[]&#123;<span class="number">20</span>, <span class="number">10</span>&#125;, <span class="number">0</span>);  </span><br><span class="line">PathEffect discreteEffect = <span class="keyword">new</span> <span class="type">DiscretePathEffect</span>(<span class="number">20</span>, <span class="number">5</span>);  </span><br><span class="line">pathEffect = <span class="keyword">new</span> <span class="type">ComposePathEffect</span>(dashEffect, discreteEffect);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawPath(path, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7epf94aj30dr05eq2x.jpg" alt=""></p><p>它的构造方法 <code>ComposePathEffect(PathEffect outerpe, PathEffect innerpe)</code> 中的两个 <code>PathEffect</code> 参数， <code>innerpe</code> 是先应用的， <code>outerpe</code> 是后应用的。所以上面的代码就是「先偏离，再变虚线」。而如果把两个参数调换，就成了「先变虚线，再偏离」。至于具体的视觉效果……我就不贴图了，你自己试试看吧！</p><p>上面这些就是 <code>Paint</code> 中的 6 种 <code>PathEffect</code>。它们有的是有独立效果的，有的是用来组合不同的 <code>PathEffect</code> 的，功能各不一样。</p><blockquote><p>注意： <code>PathEffect</code> 在有些情况下不支持硬件加速，需要关闭硬件加速才能正常使用：</p><ol><li><code>Canvas.drawLine()</code> 和 <code>Canvas.drawLines()</code> 方法画直线时，<code>setPathEffect()</code> 是不支持硬件加速的；</li><li><code>PathDashPathEffect</code> 对硬件加速的支持也有问题，所以当使用 <code>PathDashPathEffect</code> 的时候，最好也把硬件加速关了。</li></ol></blockquote><p>剩下的两个效果类方法：<code>setShadowLayer()</code> 和 <code>setMaskFilter()</code> ，它们和前面的效果类方法有点不一样：它们设置的是「附加效果」，也就是基于在绘制内容的额外效果。</p><h3 id="2-6-setShadowLayer-float-radius-float-dx-float-dy-int-shadowColor"><a href="#2-6-setShadowLayer-float-radius-float-dx-float-dy-int-shadowColor" class="headerlink" title="2.6 setShadowLayer(float radius, float dx, float dy, int shadowColor)"></a>2.6 setShadowLayer(float radius, float dx, float dy, int shadowColor)</h3><p>在之后的绘制内容下面加一层阴影。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setShadowLayer(<span class="number">10</span>, <span class="number">0</span>, <span class="number">0</span>, Color.RED);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawText(text, <span class="number">80</span>, <span class="number">300</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7ev8io8j30cv02y74f.jpg" alt=""></p><p>效果就是上面这样。方法的参数里， <code>radius</code> 是阴影的模糊范围； <code>dx</code> <code>dy</code> 是阴影的偏移量； <code>shadowColor</code> 是阴影的颜色。</p><p>如果要清除阴影层，使用 <code>clearShadowLayer()</code> 。</p><p>注意：</p><ul><li><p>在硬件加速开启的情况下， <code>setShadowLayer()</code> 只支持文字的绘制，文字之外的绘制必须关闭硬件加速才能正常绘制阴影。</p></li><li><p>如果 <code>shadowColor</code> 是半透明的，阴影的透明度就使用 <code>shadowColor</code> 自己的透明度；而如果 <code>shadowColor</code> 是不透明的，阴影的透明度就使用 <code>paint</code> 的透明度。</p></li></ul><h3 id="2-7-setMaskFilter-MaskFilter-maskfilter"><a href="#2-7-setMaskFilter-MaskFilter-maskfilter" class="headerlink" title="2.7 setMaskFilter(MaskFilter maskfilter)"></a>2.7 setMaskFilter(MaskFilter maskfilter)</h3><p>为之后的绘制设置 <code>MaskFilter</code>。上一个方法 <code>setShadowLayer()</code> 是设置的在绘制层下方的附加效果；而这个 <code>MaskFilter</code> 和它相反，设置的是在绘制层上方的附加效果。</p><blockquote><p>到现在已经有两个 <code>setXxxFilter(filter)</code> 了。前面有一个 <code>setColorFilter(filter)</code> ，是对每个像素的颜色进行过滤；而这里的 <code>setMaskFilter(filter)</code> 则是基于整个画面来进行过滤。</p></blockquote><p><code>MaskFilter</code> 有两种： <code>BlurMaskFilter</code> 和 <code>EmbossMaskFilter</code>。</p><h4 id="2-7-1-BlurMaskFilter"><a href="#2-7-1-BlurMaskFilter" class="headerlink" title="2.7.1 BlurMaskFilter"></a>2.7.1 BlurMaskFilter</h4><p>模糊效果的 <code>MaskFilter</code>。</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">paint</span><span class="selector-class">.setMaskFilter</span>(<span class="selector-tag">new</span> <span class="selector-tag">BlurMaskFilter</span>(50, <span class="selector-tag">BlurMaskFilter</span><span class="selector-class">.Blur</span><span class="selector-class">.NORMAL</span>));</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">canvas</span><span class="selector-class">.drawBitmap</span>(<span class="selector-tag">bitmap</span>, 100, 100, <span class="selector-tag">paint</span>);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7f17yu0j30kl0b2q3w.jpg" alt=""></p><p>它的构造方法 <code>BlurMaskFilter(float radius, BlurMaskFilter.Blur style)</code> 中， <code>radius</code> 参数是模糊的范围， <code>style</code> 是模糊的类型。一共有四种：</p><ul><li><code>NORMAL</code>: 内外都模糊绘制</li><li><code>SOLID</code>: 内部正常绘制，外部模糊</li><li><code>INNER</code>: 内部模糊，外部不绘制</li><li><code>OUTER</code>: 内部不绘制，外部模糊（什么鬼？）</li></ul><p><img src="images/006tNc79ly1fig7fr4dwgj30lk0mbgne.jpg" alt=""></p><h4 id="2-7-2-EmbossMaskFilter"><a href="#2-7-2-EmbossMaskFilter" class="headerlink" title="2.7.2 EmbossMaskFilter"></a>2.7.2 EmbossMaskFilter</h4><p>浮雕效果的 <code>MaskFilter</code>。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setMaskFilter(new EmbossMaskFilter(new <span class="type">float</span>[]&#123;<span class="number">0</span>, <span class="number">1</span>, <span class="number">1</span>&#125;, <span class="number">0.2</span>f, <span class="number">8</span>, <span class="number">10</span>));</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">canvas.drawBitmap(bitmap, <span class="number">100</span>, <span class="number">100</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="images/006tNc79ly1fig7g3ktlmj30kp09g0tl.jpg" alt=""></p><p>它的构造方法 <code>EmbossMaskFilter(float[] direction, float ambient, float specular, float blurRadius)</code> 的参数里， <code>direction</code> 是一个 3 个元素的数组，指定了光源的方向； <code>ambient</code> 是环境光的强度，数值范围是 0 到 1； <code>specular</code> 是炫光的系数； <code>blurRadius</code> 是应用光线的范围。</p><p>不过由于我没有在项目中使用过 <code>EmbossMaskFilter</code>，对它的每个参数具体调节方式并不熟，你有兴趣的话自己研究一下吧。</p><h3 id="2-8-获取绘制的-Path"><a href="#2-8-获取绘制的-Path" class="headerlink" title="2.8 获取绘制的 Path"></a>2.8 获取绘制的 Path</h3><p>这是效果类的最后一组方法，也是效果类唯一的一组 <code>get</code> 方法。</p><p>这组方法做的事是，根据 <code>paint</code> 的设置，计算出绘制 <code>Path</code> 或文字时的<strong>实际 Path</strong>。</p><p>这里你可能会冒出两个问题：</p><ol><li>什么叫「实际 <code>Path</code>」？ <code>Path</code> 就是 <code>Path</code>，这加上个「实际」是什么意思？</li><li>文字的 <code>Path</code> ？文字还有 <code>Path</code>？</li></ol><p>这两个问题（咦好像有四个问号）的答案就在后面的内容里。</p><h4 id="2-8-1-getFillPath-Path-src-Path-dst"><a href="#2-8-1-getFillPath-Path-src-Path-dst" class="headerlink" title="2.8.1 getFillPath(Path src, Path dst)"></a>2.8.1 getFillPath(Path src, Path dst)</h4><p>首先解答第一个问题：「实际 <code>Path</code>」。所谓实际 <code>Path</code> ，指的就是 <code>drawPath()</code> 的绘制内容的轮廓，要算上线条宽度和设置的 <code>PathEffect</code>。</p><p>默认情况下（线条宽度为 0、没有 <code>PathEffect</code>），原 <code>Path</code> 和实际 <code>Path</code> 是一样的；而在线条宽度不为 0 （并且模式为 <code>STROKE</code> 模式或 <code>FLL_AND_STROKE</code> ），或者设置了 <code>PathEffect</code> 的时候，实际 <code>Path</code> 就和原 <code>Path</code> 不一样了：</p><p><img src="images/006tNc79ly1fig7ggbut0j30rw0me76k.jpg" alt=""></p><blockquote><p>看明白了吗？</p></blockquote><p>通过 <code>getFillPath(src, dst)</code> 方法就能获取这个实际 <code>Path</code>。方法的参数里，<code>src</code> 是原 <code>Path</code> ，而 <code>dst</code>就是实际 <code>Path</code> 的保存位置。 <code>getFillPath(src, dst)</code> 会计算出实际 <code>Path</code>，然后把结果保存在 <code>dst</code>里。</p><h4 id="2-8-2-getTextPath-String-text-int-start-int-end-float-x-float-y-Path-path-getTextPath-char-text-int-index-int-count-float-x-float-y-Path-path"><a href="#2-8-2-getTextPath-String-text-int-start-int-end-float-x-float-y-Path-path-getTextPath-char-text-int-index-int-count-float-x-float-y-Path-path" class="headerlink" title="2.8.2 getTextPath(String text, int start, int end, float x, float y, Path path) / getTextPath(char[] text, int index, int count, float x, float y, Path path)"></a>2.8.2 getTextPath(String text, int start, int end, float x, float y, Path path) / getTextPath(char[] text, int index, int count, float x, float y, Path path)</h4><p>这里就回答第二个问题：「文字的 <code>Path</code>」。文字的绘制，虽然是使用 <code>Canvas.drawText()</code> 方法，但其实在下层，文字信息全是被转化成图形，对图形进行绘制的。 <code>getTextPath()</code> 方法，获取的就是目标文字所对应的 <code>Path</code> 。这个就是所谓「文字的 <code>Path</code>」。</p><p><img src="images/006tNc79ly1fig7gs0dc1j30i005mq3f.jpg" alt=""></p><p>这两个方法， <code>getFillPath()</code> 和 <code>getTextPath()</code> ，就是获取绘制的 <code>Path</code> 的方法。之所以把它们归类到「效果」类方法，是因为它们主要是用于图形和文字的装饰效果的位置计算，比如<a href="https://medium.com/google-developers/a-better-underline-for-android-90ba3a2e4fb" target="_blank" rel="noopener">自定义的下划线效果</a>。</p><p><img src="images/006tNc79ly1fig7h4hk1kj30d506q3yw.jpg" alt=""></p><p>到此为止， <code>Paint</code> 的第二类方法——效果类，就也介绍完了。</p><h2 id="3-drawText-相关"><a href="#3-drawText-相关" class="headerlink" title="3 drawText() 相关"></a>3 drawText() 相关</h2><p><code>Paint</code> 有些设置是文字绘制相关的，即和 <code>drawText()</code> 相关的。</p><p>比如设置文字大小：</p><p><img src="images/006tNc79ly1fig7hgo6ioj30jv089aat.jpg" alt=""></p><p>比如设置文字间隔：</p><p><img src="images/006tNc79ly1fig7hp9k9kj30js070t99.jpg" alt=""></p><p>比如设置各种文字效果：</p><p><img src="images/006tNc79ly1fig7hwqeokj30dj09oaaz.jpg" alt=""></p><p>除此之外，<code>Paint</code> 还有很多与文字绘制相关的设置或计算的方法，非常详细。不过由于太详细了，相关方法太多了（<code>Paint</code> 超过一半的方法都是 <code>drawText()</code> 相关的，算不算多？），如果放在这里讲它们的话，内容会显得有点过量。所以这一节我就不讲它们了，把它们放在下一节里单独讲。</p><h2 id="4-初始化类"><a href="#4-初始化类" class="headerlink" title="4 初始化类"></a>4 初始化类</h2><p>这一类方法很简单，它们是用来初始化 <code>Paint</code> 对象，或者是批量设置 <code>Paint</code> 的多个属性的方法。</p><h3 id="4-1-reset"><a href="#4-1-reset" class="headerlink" title="4.1 reset()"></a>4.1 reset()</h3><p>重置 <code>Paint</code> 的所有属性为默认值。相当于重新 <code>new</code> 一个，不过性能当然高一些啦。</p><h3 id="4-2-set-Paint-src"><a href="#4-2-set-Paint-src" class="headerlink" title="4.2 set(Paint src)"></a>4.2 set(Paint src)</h3><p>把 <code>src</code> 的所有属性全部复制过来。相当于调用 <code>src</code> 所有的 <code>get</code> 方法，然后调用这个 <code>Paint</code> 的对应的 <code>set</code> 方法来设置它们。</p><h3 id="4-3-setFlags-int-flags"><a href="#4-3-setFlags-int-flags" class="headerlink" title="4.3 setFlags(int flags)"></a>4.3 setFlags(int flags)</h3><p>批量设置 flags。相当于依次调用它们的 <code>set</code> 方法。例如： </p><figure class="highlight 1c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">paint.setFlags(Paint.ANTI_ALIAS_FLAG <span class="string">| Paint.DITHER_FLAG);</span></span><br></pre></td></tr></table></figure><p>这行代码，和下面这两行是等价的：</p><figure class="highlight abnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setAntiAlias(true)<span class="comment">;  </span></span><br><span class="line">paint.setDither(true)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p><code>setFlags(flags)</code> 对应的 <code>get</code> 方法是 <code>int getFlags()</code>。</p><p>好了，这些就是 <code>Paint</code> 的四类方法：<strong>颜色类</strong>、<strong>效果类</strong>、<strong>文字绘制相关</strong>以及<strong>初始化类</strong>。其中颜色类、效果类和初始化类都已经在这节里面讲过了，剩下的一类——文字绘制类，下一节单独讲。</p><p>最后再强调一遍：<strong>这期的内容没必要全部背会，只要看懂、理解，记住有这么个东西就行了。以后在用到的时候，再拐回来翻一翻就行了。</strong></p><h2 id="练习项目"><a href="#练习项目" class="headerlink" title="练习项目"></a>练习项目</h2><p>为了避免转头就忘，强烈建议你趁热打铁，做一下这个练习项目：<a href="https://github.com/hencoder/PracticeDraw2" target="_blank" rel="noopener">HenCoderPracticeDraw2</a></p><p><img src="images/006tNc79ly1fig7i3e4hej30si0fsq5f.jpg" alt=""></p><h2 id="下期预告"><a href="#下期预告" class="headerlink" title="下期预告"></a>下期预告</h2><p>下期是文字绘制专场，我将会花一整期的篇幅来详述文字的绘制。惯例放出部分配图作为预览：</p><p><img src="images/006tNc79ly1fig7icra08j30do077js1.jpg" alt=""></p><p><img src="images/006tNc79ly1fig7ifhctmj30cx09dt9o.jpg" alt=""></p><p><img src="images/006tNc79ly1fig7iipafvj30l807675k.jpg" alt=""></p><p><img src="images/006tNc79ly1fig7inhstij30ff0alt9l.jpg" alt=""></p><h2 id="感谢"><a href="#感谢" class="headerlink" title="感谢"></a>感谢</h2><p>感谢参与这期预发布内测的读者：</p><p><a href="https://github.com/CodeXiaoMai" target="_blank" rel="noopener">小迈</a>、<a href="https://github.com/MadisonRong" target="_blank" rel="noopener">MadisonRong</a>、小于、恋上你的眸、<a href="http://blog.csdn.net/my_truelove" target="_blank" rel="noopener">rubicAndroid</a>、<a href="https://github.com/miaoyongjun" target="_blank" rel="noopener">miaoyongjun</a>、<a href="https://github.com/ArchyWang" target="_blank" rel="noopener">ArchyWang</a>、孙志帅、<a href="https://github.com/CzwAthou" target="_blank" rel="noopener">czwathou</a>、<a href="https://github.com/timaimee" target="_blank" rel="noopener">Tim Aimee</a>、<a href="http://weibo.com/u/5551674121" target="_blank" rel="noopener">code小生</a></p><p>另外，公开招募内测读者，愿意帮助内测的扫下面的码加群吧！（已经加过一群的就别加这个了，给别人留个名额，两个群待遇一样的。）</p><p><img src="images/006tNc79ly1fig7ja7it1j30hs0s1wgl.jpg" alt=""></p><h2 id="赞赏"><a href="#赞赏" class="headerlink" title="赞赏"></a>赞赏</h2><p>老规矩，但你的钱换不来任何增值服务，所以真的觉得赞再给钱哟。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;HenCoder-Android-开发进阶-自定义-View-1-2-Paint-详解&quot;&gt;&lt;a href=&quot;#HenCoder-Android-开发进阶-自定义-View-1-2-Paint-详解&quot; class=&quot;headerlink&quot; title=&quot;HenCod
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title></title>
    <link href="https://seachal.me/uncategorized/2018/01/02/Hexo/2018-01-02/"/>
    <id>https://seachal.me/uncategorized/2018/01/02/Hexo/2018-01-02/</id>
    <published>2018-01-02T06:06:22.000Z</published>
    <updated>2018-01-02T06:11:20.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Hexo遇到的坑"><a href="#Hexo遇到的坑" class="headerlink" title="Hexo遇到的坑"></a>Hexo遇到的坑</h1><p>##1.imagemin<br>使用npm install hexo-imagemin –save安装imagemin后<br>在根目录 _cofig.yml中配置imagemin如下。</p><blockquote><p>imagemin:<br>  enable     : true<br>  interlaced : false<br>  multipass  : false<br>  optimizationLevel: 2<br>  pngquant   : false<br>  progressive: false</p></blockquote><p>  会有一些图片在  <code>hexo g</code>命令后生成的是0kb，根本打不开，注释掉_cofig.yml中关于imagemin的配置，生成的图片还是0kb，使用<code>npm uninstall hexo-imagemin</code>命令卸载后，就解决了了问题。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Hexo遇到的坑&quot;&gt;&lt;a href=&quot;#Hexo遇到的坑&quot; class=&quot;headerlink&quot; title=&quot;Hexo遇到的坑&quot;&gt;&lt;/a&gt;Hexo遇到的坑&lt;/h1&gt;&lt;p&gt;##1.imagemin&lt;br&gt;使用npm install hexo-imagemin –s
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title></title>
    <link href="https://seachal.me/uncategorized/2018/01/02/HenCoder/HenCoder%20Android%20%E5%BC%80%E5%8F%91%E8%BF%9B%E9%98%B6:%20%E8%87%AA%E5%AE%9A%E4%B9%89%20View%201-1%20%E7%BB%98%E5%88%B6%E5%9F%BA%E7%A1%80/"/>
    <id>https://seachal.me/uncategorized/2018/01/02/HenCoder/HenCoder Android 开发进阶: 自定义 View 1-1 绘制基础/</id>
    <published>2018-01-02T03:50:52.000Z</published>
    <updated>2018-01-02T06:48:32.000Z</updated>
    
    <content type="html"><![CDATA[<hr><hr><p>#HenCoder Android 开发进阶: 自定义 View 1-1 绘制基础<br>从今天开始，HenCoder 就正式开讲知识技能了。按照我的计划，第一季是 UI，UI 一共分为三部分：绘制、布局和触摸反馈。本期是绘制部分的第一期。绘制大概会用 5~6 期的时间讲完，整个 UI 的绘制、布局和触摸反馈三部分大概会用十来期。更新频率大约为每周一更（不承诺哟）。</p><p>如果你不知道 HenCoder 是什么，可以先看这里：</p><p><a href="http://hencoder.com/overview" target="_blank" rel="noopener">HenCoder：给高级 Android 工程师的进阶手册</a></p><h2 id="自定义绘制概述"><a href="#自定义绘制概述" class="headerlink" title="自定义绘制概述"></a>自定义绘制概述</h2><p>二话不说，我反手就是一个视频：</p><p>首先总结一下视频中的关键点：</p><ul><li>自定义绘制的方式是重写绘制方法，其中最常用的是 onDraw()</li><li><p>绘制的关键是 Canvas 的使用 </p><ul><li>Canvas 的绘制类方法： drawXXX() （关键参数：Paint）</li><li>Canvas 的辅助类方法：范围裁切和几何变换</li></ul></li><li>可以使用不同的绘制方法来控制遮盖关系</li></ul><p>概念已经在视频里全部讲出来了，知识点并不多，但你可能也看出来了，我讲得并不细。这是因为知识点虽然不多，但细节还是很多的，仅仅靠一节分享不可能讲完。我按照顺序把这些知识分成了 4 个级别，拆成几节来讲，你按照这 4 个级别的顺序学习下来，就能够平滑地逐步进阶。</p><h2 id="自定义绘制知识的四个级别"><a href="#自定义绘制知识的四个级别" class="headerlink" title="自定义绘制知识的四个级别"></a>自定义绘制知识的四个级别</h2><ol><li><p>Canvas 的 drawXXX() 系列方法及 <code>Paint</code> 最常见的使用</p><p> <code>Canvas.drawXXX()</code> 是自定义绘制最基本的操作。掌握了这些方法，你才知道怎么绘制内容，例如怎么画圆、怎么画方、怎么画图像和文字。组合绘制这些内容，再配合上 <code>Paint</code> 的一些常见方法来对绘制内容的颜色和风格进行简单的配置，就能够应付大部分的绘制需求了。</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7m8jqt6j3080052aa0.jpg" alt=""></p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7m9i6dpj309j05hjre.jpg" alt=""></p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7mb2q0uj307206l0su.jpg" alt=""></p><p> 今天这篇分享我要讲的就是这些内容。也就是说，你在看完这篇文章<strong>并做完练习</strong>之后，上面这几幅图你就会绘制出来了。从今以后，你也很少再需要假装一本正经地对设计师说「不行这个图技术上实现不了」，也不用心惊胆战得等待设计师的那句「那 iOS 怎么可以」了。</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7mxtr50j306i07kaac.jpg" alt=""></p></li><li><p><code>Paint</code> 的完全攻略</p><p> <code>Paint</code> 可以做的事，不只是设置颜色，也不只是我在视频里讲的实心空心、线条粗细、有没有阴影，它可以做的风格设置真的是非常多、非常细。例如：</p><blockquote><p>拐角要什么形状？</p></blockquote><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7n2p161j30ch045aa3.jpg" alt=""></p><blockquote><p>开不开双线性过滤？</p></blockquote><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7n4bfpmj30ia08gt9g.jpg" alt=""></p><blockquote><p>加不加特效？</p></blockquote><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7n6s23tj30ij08iaaq.jpg" alt=""></p><p> 可以调节的非常多，我就不一一列举了。当你掌握到这个级别，就真的不会有什么东西会是 iOS 能做到但你做不到的了。就算设计师再设计出了很难做的东西，做不出来的也不再会是你们 Android 组了。</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7n9535lj307807t74k.jpg" alt=""></p></li><li><p><code>Canvas</code> 对绘制的辅助——范围裁切和几何变换。</p><p> 范围裁切：</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7ot5vscj30i70bzq3m.jpg" alt=""></p><p> 几何变换：</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7q09im3j30q20k240u.jpg" alt=""></p><p> 大多数时候，它们并不会被用到，但一旦用到，通常都是很炫酷的效果。范围裁切和几何变换都是用于辅助的，它们本身并不酷，让它们变酷的是设计师们的想象力与创造力。而你要做的，是把他们的想象力与创造力变成现实。</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7q9k5nuj30cq06zgm7.jpg" alt=""></p></li><li><p>使用不同的绘制方法来控制绘制顺序</p><p> <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7qkonwrj30cb07cjry.jpg" alt=""></p><p> 控制绘制顺序解决的并不是「做不到」的问题，而是性能问题。同样的一种效果，你不用绘制顺序的控制往往也能做到，但需要用多个 View 甚至是多层 View 才能拼凑出来，因此代价是 UI 的性能；而使用绘制顺序的控制的话，一个 View 就全部搞定了。</p></li></ol><p>自定义绘制的知识，大概就分为上面这四个级别。在你把这四个级别依次掌握了之后，你就是一个自定义绘制的高手了。它们具体的细节，我将分成几篇来讲。今天这篇就是第一篇： <code>Canvas.drawXXX()</code> 系列方法及 <code>Paint</code> 最基本的使用。我要正式开始喽？</p><h2 id="一切的开始：onDraw"><a href="#一切的开始：onDraw" class="headerlink" title="一切的开始：onDraw()"></a>一切的开始：onDraw()</h2><p>自定义绘制的上手非常容易：提前创建好 <code>Paint</code> 对象，重写 <code>onDraw()</code>，把绘制代码写在 <code>onDraw()</code> 里面，就是自定义绘制最基本的实现。大概就像这样：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Paint paint = <span class="keyword">new</span> Paint();</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">protected</span> <span class="function"><span class="keyword">void</span> <span class="title">onDraw</span><span class="params">(Canvas canvas)</span> </span>&#123;  </span><br><span class="line">    <span class="keyword">super</span>.onDraw(canvas);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 绘制一个圆</span></span><br><span class="line">    canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就这么简单。所以关于 <code>onDraw()</code> 其实没什么好说的，一个很普通的方法重写，唯一需要注意的是别漏写了 <code>super.onDraw()</code>。</p><h2 id="Canvas-drawXXX-和-Paint-基础"><a href="#Canvas-drawXXX-和-Paint-基础" class="headerlink" title="Canvas.drawXXX() 和 Paint 基础"></a>Canvas.drawXXX() 和 Paint 基础</h2><p><code>drawXXX()</code> 系列方法和 <code>Paint</code> 的基础掌握了，就能够应付简单的绘制需求。它们主要包括：</p><ol><li><code>Canvas</code> 类下的所有 <code>draw-</code> 打头的方法，例如 <code>drawCircle()</code> <code>drawBitmap()</code>。</li><li><p><code>Paint</code> 类的几个最常用的方法。具体是： </p><ul><li><code>Paint.setStyle(Style style)</code> 设置绘制模式</li><li><code>Paint.setColor(int color)</code> 设置颜色</li><li><code>Paint.setStrokeWidth(float width)</code> 设置线条宽度</li><li><code>Paint.setTextSize(float textSize)</code> 设置文字大小</li><li><code>Paint.setAntiAlias(boolean aa)</code> 设置抗锯齿开关</li></ul></li></ol><p>对于比较习惯于自学的人（我就是这样的人），你看到这里就已经可以去 Google 的官方文档里，打开 <a href="https://developer.android.com/reference/android/graphics/Canvas.html" target="_blank" rel="noopener">Canvas</a> 和 <a href="https://developer.android.com/reference/android/graphics/Paint.html" target="_blank" rel="noopener">Paint</a> 的页面，把上面的这两类方法学习一下，然后今天的内容就算结束了。当然，这篇文章也可以关掉了。</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7r0l4bvj306k07omxb.jpg" alt=""></p><p>下面的内容就是展开讲解上面的这两类方法。</p><h3 id="Canvas-drawColor-ColorInt-int-color-颜色填充"><a href="#Canvas-drawColor-ColorInt-int-color-颜色填充" class="headerlink" title="Canvas.drawColor(@ColorInt int color) 颜色填充"></a>Canvas.drawColor(@ColorInt int color) 颜色填充</h3><p>这是最基本的 <code>drawXXX()</code> 方法：在整个绘制区域统一涂上指定的颜色。</p><p>例如 <code>drawColor(Color.BLACK)</code> 会把整个区域染成纯黑色，覆盖掉原有内容； <code>drawColor(Color.parse(&quot;#88880000&quot;)</code> 会在原有的绘制效果上加一层半透明的红色遮罩。</p><figure class="highlight gcode"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">drawColor<span class="comment">(Color.BLACK)</span>;  <span class="comment">// 纯黑</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7rts3gnj30cn062wel.jpg" alt=""></p><figure class="highlight gauss"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">drawColor(Color.<span class="built_in">parse</span>(<span class="string">"#88880000"</span>); <span class="comment">// 半透明红色</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7rvg3dbj30cg061mxa.jpg" alt=""></p><p>类似的方法还有 <code>drawRGB(int r, int g, int b)</code> 和 <code>drawARGB(int a, int r, int g, int b)</code> ，它们和 <code>drawColor(color)</code> 只是使用方式不同，作用都是一样的。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">canvas.drawRGB(<span class="number">100</span>, <span class="number">200</span>, <span class="number">100</span>);  </span><br><span class="line">canvas.drawARGB(<span class="number">100</span>, <span class="number">100</span>, <span class="number">200</span>, <span class="number">100</span>);</span><br></pre></td></tr></table></figure><p>这类颜色填充方法一般用于在绘制之前设置底色，或者在绘制之后为界面设置半透明蒙版。</p><h3 id="drawCircle-float-centerX-float-centerY-float-radius-Paint-paint-画圆"><a href="#drawCircle-float-centerX-float-centerY-float-radius-Paint-paint-画圆" class="headerlink" title="drawCircle(float centerX, float centerY, float radius, Paint paint) 画圆"></a>drawCircle(float centerX, float centerY, float radius, Paint paint) 画圆</h3><p>前两个参数 <code>centerX</code> <code>centerY</code> 是圆心的坐标，第三个参数 <code>radius</code> 是圆的半径，单位都是像素，它们共同构成了这个圆的基本信息（即用这几个信息可以构建出一个确定的圆）；第四个参数 <code>paint</code> 我在视频里面已经说过了，它提供基本信息之外的所有风格信息，例如颜色、线条粗细、阴影等。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7spahxyj307707djrc.jpg" alt=""></p><p>那位说：「你等会儿！先别往后讲，你刚才说圆心的坐标，我想问坐标系在哪儿呢？没坐标系你跟我聊什么坐标啊。」</p><p>我想说：问得好（强行插入剧情）。在 Android 里，每个 View 都有一个自己的坐标系，彼此之间是不影响的。这个坐标系的原点是 View 左上角的那个点；水平方向是 x 轴，右正左负；竖直方向是 y 轴，下正上负（注意，是下正上负，不是上正下负，和上学时候学的坐标系方向不一样）。也就是下面这个样子。</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7syr2ghj30h40etdfz.jpg" alt=""></p><p>所以一个 View 的坐标 (x, y) 处，指的就是相对它的左上角那个点的水平方向 x 像素、竖直方向 y 像素的点。例如，(300, 300) 指的就是左上角的点向右 300 、向下 300 的位置； (100, -50) 指的就是左上角的点向右 100 、向上 50 的位置。</p><p>也就是说， <code>canvas.drawCircle(300, 300, 200, paint)</code> 这行代码绘制出的圆，在 View 中的位置和尺寸应该是这样的：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7t79a4oj30h00eqaah.jpg" alt=""></p><p>圆心坐标和半径，这些都是圆的基本信息，也是它的独有信息。什么叫独有信息？就是只有它有，别人没有的信息。你画圆有圆心坐标和半径，画方有吗？画椭圆有吗？这就叫独有信息。独有信息都是直接作为参数写进 <code>drawXXX()</code> 方法里的（比如 <code>drawCircle(centerX, centerY, radius, paint)</code> 的前三个参数）。</p><p>而除此之外，其他的都是公有信息。比如图形的颜色、空心实心这些，你不管是画圆还是画方都有可能用到的，这些信息则是统一放在 <code>paint</code> 参数里的。</p><h5 id="插播一：-Paint-setColor-int-color"><a href="#插播一：-Paint-setColor-int-color" class="headerlink" title="插播一： Paint.setColor(int color)"></a>插播一： Paint.setColor(int color)</h5><p>例如，你要画一个红色的圆，并不是写成 <code>canvas.drawCircle(300, 300, 200, RED, paint)</code> 这样，而是像下面这样：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setColor(Color.RED); <span class="comment">// 设置为红色  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7tezf9aj304g03xjra.jpg" alt=""></p><p><code>Paint.setColor(int color)</code> 是 <code>Paint</code> 最常用的方法之一，用来设置绘制内容的颜色。你不止可以用它画红色的圆，也可以用它来画红色的矩形、红色的五角星、红色的文字。</p><h5 id="插播二：-Paint-setStyle-Paint-Style-style"><a href="#插播二：-Paint-setStyle-Paint-Style-style" class="headerlink" title="插播二： Paint.setStyle(Paint.Style style)"></a>插播二： Paint.setStyle(Paint.Style style)</h5><p>而如果你想画的不是实心圆，而是空心圆（或者叫环形），也可以使用 <code>paint.setStyle(Paint.Style.STROKE)</code> 来把绘制模式改为画线模式。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.STROKE); <span class="comment">// Style 修改为画线模式  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7tlng4gj303z03ta9y.jpg" alt=""></p><p><code>setStyle(Style style)</code> 这个方法设置的是绘制的 <code>Style</code> 。<code>Style</code> 具体来说有三种： <code>FILL</code>, <code>STROKE</code>和 <code>FILL_AND_STROKE</code> 。<code>FILL</code> 是填充模式，<code>STROKE</code> 是画线模式（即勾边模式），<code>FILL_AND_STROKE</code> 是两种模式一并使用：既画线又填充。它的默认值是 <code>FILL</code>，填充模式。</p><h5 id="插播三：-Paint-setStrokeWidth-float-width"><a href="#插播三：-Paint-setStrokeWidth-float-width" class="headerlink" title="插播三： Paint.setStrokeWidth(float width)"></a>插播三： Paint.setStrokeWidth(float width)</h5><p>在 <code>STROKE</code> 和 <code>FILL_AND_STROKE</code> 下，还可以使用 <code>paint.setStrokeWidth(float width)</code> 来设置线条的宽度：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.STROKE);  </span><br><span class="line">paint.setStrokeWidth(<span class="number">20</span>); <span class="comment">// 线条宽度为 20 像素  </span></span><br><span class="line">canvas.drawCircle(<span class="number">300</span>, <span class="number">300</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7trxdfoj304l048q2v.jpg" alt=""></p><h5 id="插播四：-抗锯齿"><a href="#插播四：-抗锯齿" class="headerlink" title="插播四： 抗锯齿"></a>插播四： 抗锯齿</h5><p>在绘制的时候，往往需要开启抗锯齿来让图形和文字的边缘更加平滑。开启抗锯齿很简单，只要在 <code>new Paint()</code> 的时候加上一个 <code>ANTI_ALIAS_FLAG</code> 参数就行：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">Paint paint</span> = new Paint(Paint.ANTI_ALIAS_FLAG);</span><br></pre></td></tr></table></figure><p>另外，你也可以使用 <code>Paint.setAntiAlias(boolean aa)</code> 来动态开关抗锯齿。</p><p>抗锯齿的效果如下：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7u3idq8j30bx06pjrk.jpg" alt=""></p><p>可以看出，没有开启抗锯齿的时候，图形会有毛片现象，啊不，毛边现象。所以一定记得要打开抗锯齿哟！</p><blockquote><p><em>可以跳过的冷知识</em></p><p>好奇的人可能会问：抗锯齿既然这么有用，为什么不默认开启，或者干脆把这个开关取消，自动让所有绘制都开启抗锯齿？</p><p>短答案：因为抗锯齿并不一定适合所有场景。</p><p>长答案：所谓的毛边或者锯齿，发生的原因并不是很多人所想象的「绘制太粗糙」「像素计算能力不足」；同样，抗锯齿的原理也并不是选择了更精细的算法来算出了更平滑的图形边缘。<br>实质上，锯齿现象的发生，只是由于图形分辨率过低，导致人眼察觉出了画面中的像素颗粒而已。换句话说，就算不开启抗锯齿，图形的边缘也已经是最完美的了，而并不是一个粗略计算的粗糙版本。<br>那么，为什么抗锯齿开启之后的图形边缘会更加平滑呢？因为抗锯齿的原理是：修改图形边缘处的像素颜色，从而<strong>让图形在肉眼看来具有更加平滑的感觉</strong>。一图胜千言，上图：<br><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7uen44ej30ce07274d.jpg" alt=""><br>上面这个是把前面那两个圆放大后的局部效果。看到没有？未开启抗锯齿的圆，所有像素都是同样的黑色，而开启了抗锯齿的圆，边缘的颜色被略微改变了。这种改变可以让人眼有边缘平滑的感觉，但从某种角度讲，它也造成了图形的颜色失真。<br>所以，抗锯齿好不好？好，大多数情况下它都应该是开启的；但在极少数的某些时候，你还真的需要把它关闭。「某些时候」是什么时候？到你用到的时候自然就知道了。</p></blockquote><p>除了圆，<code>Canvas</code> 还可以绘制一些别的简单图形。它们的使用方法和 <code>drawCircle()</code> 大同小异，我就只对它们的 API 做简单的介绍，不再做详细的讲解。</p><h3 id="drawRect-float-left-float-top-float-right-float-bottom-Paint-paint-画矩形"><a href="#drawRect-float-left-float-top-float-right-float-bottom-Paint-paint-画矩形" class="headerlink" title="drawRect(float left, float top, float right, float bottom, Paint paint) 画矩形"></a>drawRect(float left, float top, float right, float bottom, Paint paint) 画矩形</h3><p><code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 是矩形四条边的坐标。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.FILL);  </span><br><span class="line">canvas.drawRect(<span class="number">100</span>, <span class="number">100</span>, <span class="number">500</span>, <span class="number">500</span>, paint);</span><br><span class="line"></span><br><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">canvas.drawRect(<span class="number">700</span>, <span class="number">100</span>, <span class="number">1100</span>, <span class="number">500</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7ulrq1sj309h04mgli.jpg" alt=""></p><p>另外，它还有两个重载方法 <code>drawRect(RectF rect, Paint paint)</code> 和 <code>drawRect(Rect rect, Paint paint)</code> ，让你可以直接填写 <code>RectF</code> 或 <code>Rect</code> 对象来绘制矩形。</p><h3 id="drawPoint-float-x-float-y-Paint-paint-画点"><a href="#drawPoint-float-x-float-y-Paint-paint-画点" class="headerlink" title="drawPoint(float x, float y, Paint paint) 画点"></a>drawPoint(float x, float y, Paint paint) 画点</h3><p><code>x</code> 和 <code>y</code> 是点的坐标。点的大小可以通过 <code>paint.setStrokeWidth(width)</code> 来设置；点的形状可以通过 <code>paint.setStrokeCap(cap)</code> 来设置：<code>ROUND</code> 画出来是圆形的点，<code>SQUARE</code> 或 <code>BUTT</code> 画出来是方形的点。（点还有形状？是的，反正 Google 是这么说的，你要问问 Google 去，我也很懵逼。）</p><blockquote><p>注：<code>Paint.setStrokeCap(cap)</code> 可以设置点的形状，但这个方法并不是专门用来设置点的形状的，而是一个设置线条端点形状的方法。端点有圆头 (<code>ROUND</code>)、平头 (<code>BUTT</code>) 和方头 (<code>SQUARE</code>) 三种，具体会在下一节里面讲。</p></blockquote><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">paint</span><span class="selector-class">.setStrokeWidth</span>(20);  </span><br><span class="line"><span class="selector-tag">paint</span><span class="selector-class">.setStrokeCap</span>(<span class="selector-tag">Paint</span><span class="selector-class">.Cap</span><span class="selector-class">.ROUND</span>);  </span><br><span class="line"><span class="selector-tag">canvas</span><span class="selector-class">.drawPoint</span>(50, 50, <span class="selector-tag">paint</span>);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7uwrynsj303m034jr8.jpg" alt=""></p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">paint</span><span class="selector-class">.setStrokeWidth</span>(20);  </span><br><span class="line"><span class="selector-tag">paint</span><span class="selector-class">.setStrokeCap</span>(<span class="selector-tag">Paint</span><span class="selector-class">.Cap</span><span class="selector-class">.SQUARE</span>);  </span><br><span class="line"><span class="selector-tag">canvas</span><span class="selector-class">.drawPoint</span>(50, 50, <span class="selector-tag">paint</span>);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7uy2w1ij303x02zdfo.jpg" alt=""></p><p>好像有点像 <code>FILL</code> 模式下的 <code>drawCircle()</code> 和 <code>drawRect()</code> ？事实上确实是这样的，它们和 <code>drawPoint()</code> 的绘制效果没有区别。各位在使用的时候按个人习惯和实际场景来吧，哪个方便和顺手用哪个。</p><h3 id="drawPoints-float-pts-int-offset-int-count-Paint-paint-drawPoints-float-pts-Paint-paint-画点（批量）"><a href="#drawPoints-float-pts-int-offset-int-count-Paint-paint-drawPoints-float-pts-Paint-paint-画点（批量）" class="headerlink" title="drawPoints(float[] pts, int offset, int count, Paint paint) / drawPoints(float[] pts, Paint paint) 画点（批量）"></a>drawPoints(float[] pts, int offset, int count, Paint paint) / drawPoints(float[] pts, Paint paint) 画点（批量）</h3><p>同样是画点，它和 <code>drawPoint()</code> 的区别是可以画多个点。<code>pts</code> 这个数组是点的坐标，每两个成一对；<code>offset</code> 表示跳过数组的前几个数再开始记坐标；<code>count</code> 表示一共要绘制几个点。说这么多你可能越读越晕，你还是自己试试吧，这是个看着复杂用着简单的方法。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">float</span>[] points = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">50</span>, <span class="number">50</span>, <span class="number">50</span>, <span class="number">100</span>, <span class="number">100</span>, <span class="number">50</span>, <span class="number">100</span>, <span class="number">100</span>, <span class="number">150</span>, <span class="number">50</span>, <span class="number">150</span>, <span class="number">100</span>&#125;;  </span><br><span class="line"><span class="comment">// 绘制四个点：(50, 50) (50, 100) (100, 50) (100, 100)</span></span><br><span class="line">canvas.drawPoints(points, <span class="number">2</span> <span class="comment">/* 跳过两个数，即前两个 0 */</span>,  </span><br><span class="line">          <span class="number">8</span> <span class="comment">/* 一共绘制 8 个数（4 个点）*/</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7v7v3mej305j05b746.jpg" alt=""></p><h3 id="drawOval-float-left-float-top-float-right-float-bottom-Paint-paint-画椭圆"><a href="#drawOval-float-left-float-top-float-right-float-bottom-Paint-paint-画椭圆" class="headerlink" title="drawOval(float left, float top, float right, float bottom, Paint paint) 画椭圆"></a>drawOval(float left, float top, float right, float bottom, Paint paint) 画椭圆</h3><p>只能绘制横着的或者竖着的椭圆，不能绘制斜的（斜的倒是也可以，但不是直接使用 <code>drawOval()</code>，而是配合几何变换，后面会讲到）。<code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 是这个椭圆的左、上、右、下四个边界点的坐标。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.FILL);  </span><br><span class="line">canvas.drawOval(<span class="number">50</span>, <span class="number">50</span>, <span class="number">350</span>, <span class="number">200</span>, paint);</span><br><span class="line"></span><br><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">canvas.drawOval(<span class="number">400</span>, <span class="number">50</span>, <span class="number">700</span>, <span class="number">200</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7vcxlv4j30do03t74a.jpg" alt=""></p><p>另外，它还有一个重载方法 <code>drawOval(RectF rect, Paint paint)</code>，让你可以直接填写 <code>RectF</code> 来绘制椭圆。</p><h3 id="drawLine-float-startX-float-startY-float-stopX-float-stopY-Paint-paint-画线"><a href="#drawLine-float-startX-float-startY-float-stopX-float-stopY-Paint-paint-画线" class="headerlink" title="drawLine(float startX, float startY, float stopX, float stopY, Paint paint) 画线"></a>drawLine(float startX, float startY, float stopX, float stopY, Paint paint) 画线</h3><p><code>startX</code>, <code>startY</code>, <code>stopX</code>, <code>stopY</code> 分别是线的起点和终点坐标。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.drawLine(<span class="number">200</span>, <span class="number">200</span>, <span class="number">800</span>, <span class="number">500</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7vhwyxfj306504fjra.jpg" alt=""></p><blockquote><p>由于直线不是封闭图形，所以 <code>setStyle(style)</code> 对直线没有影响。</p></blockquote><h3 id="drawLines-float-pts-int-offset-int-count-Paint-paint-drawLines-float-pts-Paint-paint-画线（批量）"><a href="#drawLines-float-pts-int-offset-int-count-Paint-paint-drawLines-float-pts-Paint-paint-画线（批量）" class="headerlink" title="drawLines(float[] pts, int offset, int count, Paint paint) / drawLines(float[] pts, Paint paint) 画线（批量）"></a>drawLines(float[] pts, int offset, int count, Paint paint) / drawLines(float[] pts, Paint paint) 画线（批量）</h3><p><code>drawLines()</code> 是 <code>drawLine()</code> 的复数版。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">float</span>[] points = &#123;<span class="number">20</span>, <span class="number">20</span>, <span class="number">120</span>, <span class="number">20</span>, <span class="number">70</span>, <span class="number">20</span>, <span class="number">70</span>, <span class="number">120</span>, <span class="number">20</span>, <span class="number">120</span>, <span class="number">120</span>, <span class="number">120</span>, <span class="number">150</span>, <span class="number">20</span>, <span class="number">250</span>, <span class="number">20</span>, <span class="number">150</span>, <span class="number">20</span>, <span class="number">150</span>, <span class="number">120</span>, <span class="number">250</span>, <span class="number">20</span>, <span class="number">250</span>, <span class="number">120</span>, <span class="number">150</span>, <span class="number">120</span>, <span class="number">250</span>, <span class="number">120</span>&#125;;  </span><br><span class="line">canvas.drawLines(points, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7vqiyl7j308v04hglk.jpg" alt=""></p><blockquote><p>咦，不小心打出两个汉字。——是汉字吧？</p></blockquote><h3 id="drawRoundRect-float-left-float-top-float-right-float-bottom-float-rx-float-ry-Paint-paint-画圆角矩形"><a href="#drawRoundRect-float-left-float-top-float-right-float-bottom-float-rx-float-ry-Paint-paint-画圆角矩形" class="headerlink" title="drawRoundRect(float left, float top, float right, float bottom, float rx, float ry, Paint paint) 画圆角矩形"></a>drawRoundRect(float left, float top, float right, float bottom, float rx, float ry, Paint paint) 画圆角矩形</h3><p><code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 是四条边的坐标，<code>rx</code> 和 <code>ry</code> 是圆角的横向半径和纵向半径。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.drawRoundRect(<span class="number">100</span>, <span class="number">100</span>, <span class="number">500</span>, <span class="number">300</span>, <span class="number">50</span>, <span class="number">50</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7vvi7asj30e104o0sp.jpg" alt=""></p><p>另外，它还有一个重载方法 <code>drawRoundRect(RectF rect, float rx, float ry, Paint paint)</code>，让你可以直接填写 <code>RectF</code> 来绘制圆角矩形。</p><h3 id="drawArc-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-boolean-useCenter-Paint-paint-绘制弧形或扇形"><a href="#drawArc-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-boolean-useCenter-Paint-paint-绘制弧形或扇形" class="headerlink" title="drawArc(float left, float top, float right, float bottom, float startAngle, float sweepAngle, boolean useCenter, Paint paint) 绘制弧形或扇形"></a>drawArc(float left, float top, float right, float bottom, float startAngle, float sweepAngle, boolean useCenter, Paint paint) 绘制弧形或扇形</h3><p><code>drawArc()</code> 是使用一个椭圆来描述弧形的。<code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code> 描述的是这个弧形所在的椭圆；<code>startAngle</code> 是弧形的起始角度（x 轴的正向，即正右的方向，是 0 度的位置；顺时针为正角度，逆时针为负角度），<code>sweepAngle</code> 是弧形划过的角度；<code>useCenter</code> 表示是否连接到圆心，如果不连接到圆心，就是弧形，如果连接到圆心，就是扇形。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Paint.Style.FILL); <span class="comment">// 填充模式  </span></span><br><span class="line">canvas.drawArc(<span class="number">200</span>, <span class="number">100</span>, <span class="number">800</span>, <span class="number">500</span>, <span class="number">-110</span>, <span class="number">100</span>, true, paint); <span class="comment">// 绘制扇形  </span></span><br><span class="line">canvas.drawArc(<span class="number">200</span>, <span class="number">100</span>, <span class="number">800</span>, <span class="number">500</span>, <span class="number">20</span>, <span class="number">140</span>, false, paint); <span class="comment">// 绘制弧形  </span></span><br><span class="line">paint.setStyle(Paint.Style.STROKE); <span class="comment">// 画线模式  </span></span><br><span class="line">canvas.drawArc(<span class="number">200</span>, <span class="number">100</span>, <span class="number">800</span>, <span class="number">500</span>, <span class="number">180</span>, <span class="number">60</span>, false, paint); <span class="comment">// 绘制不封口的弧形</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7w109ofj307904z3yg.jpg" alt=""></p><p>到此为止，以上就是 <code>Canvas</code> 所有的简单图形的绘制。除了简单图形的绘制， <code>Canvas</code> 还可以使用 <code>drawPath(Path path)</code> 来绘制自定义图形。</p><h3 id="drawPath-Path-path-Paint-paint-画自定义图形"><a href="#drawPath-Path-path-Paint-paint-画自定义图形" class="headerlink" title="drawPath(Path path, Paint paint) 画自定义图形"></a>drawPath(Path path, Paint paint) 画自定义图形</h3><p>这个方法有点复杂，需要展开说一下。</p><p>前面的这些方法，都是绘制某个给定的图形，而 <code>drawPath()</code> 可以绘制自定义图形。当你要绘制的图形比较特殊，使用前面的那些方法做不到的时候，就可以使用 <code>drawPath()</code> 来绘制。</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7w6bx32j30gi05cwej.jpg" alt=""></p><p><code>drawPath(path)</code> 这个方法是通过描述路径的方式来绘制图形的，它的 <code>path</code> 参数就是用来描述图形路径的对象。<code>path</code> 的类型是 <code>Path</code> ，使用方法大概像下面这样：</p><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">PathView</span> <span class="keyword">extends</span> <span class="title">View</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="type">Paint</span> paint = <span class="keyword">new</span> <span class="type">Paint</span>();</span><br><span class="line">    <span class="type">Path</span> path = <span class="keyword">new</span> <span class="type">Path</span>(); <span class="comment">// 初始化 Path 对象</span></span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">      <span class="comment">// 使用 path 对图形进行描述（这段描述代码不必看懂）</span></span><br><span class="line">      path.addArc(<span class="number">200</span>, <span class="number">200</span>, <span class="number">400</span>, <span class="number">400</span>, <span class="number">-225</span>, <span class="number">225</span>);</span><br><span class="line">      path.arcTo(<span class="number">400</span>, <span class="number">200</span>, <span class="number">600</span>, <span class="number">400</span>, <span class="number">-180</span>, <span class="number">225</span>, <span class="literal">false</span>);</span><br><span class="line">      path.lineTo(<span class="number">400</span>, <span class="number">542</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> void onDraw(<span class="type">Canvas</span> canvas) &#123;</span><br><span class="line">      <span class="keyword">super</span>.onDraw(canvas);</span><br><span class="line"></span><br><span class="line">      canvas.drawPath(path, paint); <span class="comment">// 绘制出 path 描述的图形（心形），大功告成</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7wdwl9cj306505ja9z.jpg" alt=""></p><p><code>Path</code> 可以描述直线、二次曲线、三次曲线、圆、椭圆、弧形、矩形、圆角矩形。把这些图形结合起来，就可以描述出很多复杂的图形。下面我就说一下具体的怎么把这些图形描述出来。</p><p><code>Path</code> 有两类方法，一类是直接描述路径的，另一类是辅助的设置或计算。</p><h4 id="Path-方法第一类：直接描述路径。"><a href="#Path-方法第一类：直接描述路径。" class="headerlink" title="Path 方法第一类：直接描述路径。"></a>Path 方法第一类：直接描述路径。</h4><p>这一类方法还可以细分为两组：添加子图形和画线（直线或曲线）</p><h5 id="第一组：-addXxx-——添加子图形"><a href="#第一组：-addXxx-——添加子图形" class="headerlink" title="第一组： addXxx() ——添加子图形"></a>第一组： <code>addXxx()</code> ——添加子图形</h5><h6 id="addCircle-float-x-float-y-float-radius-Direction-dir-添加圆"><a href="#addCircle-float-x-float-y-float-radius-Direction-dir-添加圆" class="headerlink" title="addCircle(float x, float y, float radius, Direction dir) 添加圆"></a>addCircle(float x, float y, float radius, Direction dir) 添加圆</h6><p><code>x</code>, <code>y</code>, <code>radius</code> 这三个参数是圆的基本信息，最后一个参数 <code>dir</code> 是画圆的路径的方向。</p><blockquote><p>路径方向有两种：顺时针 (<code>CW</code> clockwise) 和逆时针 (<code>CCW</code> counter-clockwise) 。对于普通情况，这个参数填 <code>CW</code> 还是填 <code>CCW</code> 没有影响。它只是在<strong>需要填充图形</strong> (<code>Paint.Style</code> 为 <code>FILL</code> 或 <code>FILL_AND_STROKE</code>) ，并且<strong>图形出现自相交</strong>时，用于判断填充范围的。比如下面这个图形：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7winr54j306m04fjrc.jpg" alt=""></p><p>是应该填充成这样呢：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7wk8r6cj306c04d747.jpg" alt=""></p><p>还是应该填充成这样呢：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7wm4nvtj306e04ddfs.jpg" alt=""></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7wrwl7bj306506cglu.jpg" alt=""></p><p>想用哪种方式来填充，都可以由你来决定。具体怎么做，下面在讲 <code>Path.setFillType()</code> 的时候我会详细介绍，而在这里你可以先忽略 <code>dir</code> 这个参数。</p></blockquote><p>在用 <code>addCircle()</code> 为 <code>Path</code> 中新增一个圆之后，调用 <code>canvas.drawPath(path, paint)</code> ，就能画一个圆出来。就像这样：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">path</span><span class="selector-class">.addCircle</span>(300, 300, 200, <span class="selector-tag">Path</span><span class="selector-class">.Direction</span><span class="selector-class">.CW</span>);</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">canvas</span><span class="selector-class">.drawPath</span>(<span class="selector-tag">path</span>, <span class="selector-tag">paint</span>);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7xh153nj303z0410sm.jpg" alt=""></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7xiorhfj304w04jgll.jpg" alt=""></p><p>可以看出，<code>path.AddCircle(x, y, radius, dir)</code> + <code>canvas.drawPath(path, paint)</code> 这种写法，和直接使用 <code>canvas.drawCircle(x, y, radius, paint)</code> 的效果是一样的，区别只是它的写法更复杂。所以如果只画一个圆，没必要用 <code>Path</code>，直接用 <code>drawCircle()</code> 就行了。<code>drawPath()</code> 一般是在绘制组合图形时才会用到的。</p><p>其他的 <code>Path.add-()</code> 方法和这类似，例如：</p><h6 id="addOval-float-left-float-top-float-right-float-bottom-Direction-dir-addOval-RectF-oval-Direction-dir-添加椭圆"><a href="#addOval-float-left-float-top-float-right-float-bottom-Direction-dir-addOval-RectF-oval-Direction-dir-添加椭圆" class="headerlink" title="addOval(float left, float top, float right, float bottom, Direction dir) / addOval(RectF oval, Direction dir) 添加椭圆"></a>addOval(float left, float top, float right, float bottom, Direction dir) / addOval(RectF oval, Direction dir) 添加椭圆</h6><h6 id="addRect-float-left-float-top-float-right-float-bottom-Direction-dir-addRect-RectF-rect-Direction-dir-添加矩形"><a href="#addRect-float-left-float-top-float-right-float-bottom-Direction-dir-addRect-RectF-rect-Direction-dir-添加矩形" class="headerlink" title="addRect(float left, float top, float right, float bottom, Direction dir) / addRect(RectF rect, Direction dir) 添加矩形"></a>addRect(float left, float top, float right, float bottom, Direction dir) / addRect(RectF rect, Direction dir) 添加矩形</h6><h6 id="addRoundRect-RectF-rect-float-rx-float-ry-Direction-dir-addRoundRect-float-left-float-top-float-right-float-bottom-float-rx-float-ry-Direction-dir-addRoundRect-RectF-rect-float-radii-Direction-dir-addRoundRect-float-left-float-top-float-right-float-bottom-float-radii-Direction-dir-添加圆角矩形"><a href="#addRoundRect-RectF-rect-float-rx-float-ry-Direction-dir-addRoundRect-float-left-float-top-float-right-float-bottom-float-rx-float-ry-Direction-dir-addRoundRect-RectF-rect-float-radii-Direction-dir-addRoundRect-float-left-float-top-float-right-float-bottom-float-radii-Direction-dir-添加圆角矩形" class="headerlink" title="addRoundRect(RectF rect, float rx, float ry, Direction dir) / addRoundRect(float left, float top, float right, float bottom, float rx, float ry, Direction dir) / addRoundRect(RectF rect, float[] radii, Direction dir) / addRoundRect(float left, float top, float right, float bottom, float[] radii, Direction dir) 添加圆角矩形"></a>addRoundRect(RectF rect, float rx, float ry, Direction dir) / addRoundRect(float left, float top, float right, float bottom, float rx, float ry, Direction dir) / addRoundRect(RectF rect, float[] radii, Direction dir) / addRoundRect(float left, float top, float right, float bottom, float[] radii, Direction dir) 添加圆角矩形</h6><h6 id="addPath-Path-path-添加另一个-Path"><a href="#addPath-Path-path-添加另一个-Path" class="headerlink" title="addPath(Path path) 添加另一个 Path"></a>addPath(Path path) 添加另一个 Path</h6><p>上面这几个方法和 <code>addCircle()</code> 的使用都差不多，不再做过多介绍。</p><h5 id="第二组：xxxTo-——画线（直线或曲线）"><a href="#第二组：xxxTo-——画线（直线或曲线）" class="headerlink" title="第二组：xxxTo() ——画线（直线或曲线）"></a>第二组：<code>xxxTo()</code> ——画线（直线或曲线）</h5><p>这一组和第一组 <code>addXxx()</code> 方法的区别在于，第一组是添加的完整封闭图形（除了 <code>addPath()</code> ），而这一组添加的只是一条线。</p><h6 id="lineTo-float-x-float-y-rLineTo-float-x-float-y-画直线"><a href="#lineTo-float-x-float-y-rLineTo-float-x-float-y-画直线" class="headerlink" title="lineTo(float x, float y) / rLineTo(float x, float y) 画直线"></a>lineTo(float x, float y) / rLineTo(float x, float y) 画直线</h6><p>从<strong>当前位置</strong>向目标位置画一条直线， <code>x</code> 和 <code>y</code> 是目标位置的坐标。这两个方法的区别是，<code>lineTo(x, y)</code>的参数是<strong>绝对坐标</strong>，而 <code>rLineTo(x, y)</code> 的参数是相对当前位置的<strong>相对坐标</strong> （前缀 <code>r</code> 指的就是 <code>relatively</code> 「相对地」)。</p><blockquote><p><strong>当前位置</strong>：所谓当前位置，即最后一次调用画 <code>Path</code> 的方法的终点位置。初始值为原点 (0, 0)。</p></blockquote><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.lineTo(<span class="number">100</span>, <span class="number">100</span>); <span class="comment">// 由当前位置 (0, 0) 向 (100, 100) 画一条直线  </span></span><br><span class="line">path.rLineTo(<span class="number">100</span>, <span class="number">0</span>); <span class="comment">// 由当前位置 (100, 100) 向正右方 100 像素的位置画一条直线</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7xtxm37j307z04z746.jpg" alt=""></p><h6 id="quadTo-float-x1-float-y1-float-x2-float-y2-rQuadTo-float-dx1-float-dy1-float-dx2-float-dy2-画二次贝塞尔曲线"><a href="#quadTo-float-x1-float-y1-float-x2-float-y2-rQuadTo-float-dx1-float-dy1-float-dx2-float-dy2-画二次贝塞尔曲线" class="headerlink" title="quadTo(float x1, float y1, float x2, float y2) / rQuadTo(float dx1, float dy1, float dx2, float dy2) 画二次贝塞尔曲线"></a>quadTo(float x1, float y1, float x2, float y2) / rQuadTo(float dx1, float dy1, float dx2, float dy2) 画二次贝塞尔曲线</h6><p>这条二次贝塞尔曲线的起点就是当前位置，而参数中的 <code>x1</code>, <code>y1</code> 和 <code>x2</code>, <code>y2</code> 则分别是控制点和终点的坐标。和 <code>rLineTo(x, y)</code> 同理，<code>rQuadTo(dx1, dy1, dx2, dy2)</code> 的参数也是相对坐标</p><blockquote><p><strong>贝塞尔曲线</strong>：贝塞尔曲线是几何上的一种曲线。它通过起点、控制点和终点来描述一条曲线，主要用于计算机图形学。概念总是说着容易听着难，总之使用它可以绘制很多圆润又好看的图形，但要把它熟练掌握、灵活使用却是不容易的。不过还好的是，一般情况下，贝塞尔曲线并没有什么用处，只在少数场景下绘制一些特殊图形的时候才会用到，所以如果你还没掌握自定义绘制，可以先把贝塞尔曲线放一放，稍后再学也完全没问题。至于怎么学，贝塞尔曲线的知识网上一搜一大把，我这里就不讲了。</p></blockquote><h6 id="cubicTo-float-x1-float-y1-float-x2-float-y2-float-x3-float-y3-rCubicTo-float-x1-float-y1-float-x2-float-y2-float-x3-float-y3-画三次贝塞尔曲线"><a href="#cubicTo-float-x1-float-y1-float-x2-float-y2-float-x3-float-y3-rCubicTo-float-x1-float-y1-float-x2-float-y2-float-x3-float-y3-画三次贝塞尔曲线" class="headerlink" title="cubicTo(float x1, float y1, float x2, float y2, float x3, float y3) / rCubicTo(float x1, float y1, float x2, float y2, float x3, float y3) 画三次贝塞尔曲线"></a>cubicTo(float x1, float y1, float x2, float y2, float x3, float y3) / rCubicTo(float x1, float y1, float x2, float y2, float x3, float y3) 画三次贝塞尔曲线</h6><p>和上面这个 <code>quadTo()</code> <code>rQuadTo()</code> 的二次贝塞尔曲线同理，<code>cubicTo()</code> 和 <code>rCubicTo()</code> 是三次贝塞尔曲线，不再解释。</p><h6 id="moveTo-float-x-float-y-rMoveTo-float-x-float-y-移动到目标位置"><a href="#moveTo-float-x-float-y-rMoveTo-float-x-float-y-移动到目标位置" class="headerlink" title="moveTo(float x, float y) / rMoveTo(float x, float y) 移动到目标位置"></a>moveTo(float x, float y) / rMoveTo(float x, float y) 移动到目标位置</h6><p>不论是直线还是贝塞尔曲线，都是以当前位置作为起点，而不能指定起点。但你可以通过 <code>moveTo(x, y)</code>或 <code>rMoveTo()</code> 来改变当前位置，从而间接地设置这些方法的起点。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.lineTo(<span class="number">100</span>, <span class="number">100</span>); <span class="comment">// 画斜线  </span></span><br><span class="line">path.moveTo(<span class="number">200</span>, <span class="number">100</span>); <span class="comment">// 我移~~  </span></span><br><span class="line">path.lineTo(<span class="number">200</span>, <span class="number">0</span>); <span class="comment">// 画竖线</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7yhwyqqj307t04k3ye.jpg" alt=""></p><p><code>moveTo(x, y)</code> 虽然不添加图形，但它会设置图形的起点，所以它是非常重要的一个辅助方法。</p><p>另外，第二组还有两个特殊的方法： <code>arcTo()</code> 和 <code>addArc()</code>。它们也是用来画线的，但并不使用当前位置作为弧线的起点。</p><h6 id="arcTo-RectF-oval-float-startAngle-float-sweepAngle-boolean-forceMoveTo-arcTo-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-boolean-forceMoveTo-arcTo-RectF-oval-float-startAngle-float-sweepAngle-画弧形"><a href="#arcTo-RectF-oval-float-startAngle-float-sweepAngle-boolean-forceMoveTo-arcTo-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-boolean-forceMoveTo-arcTo-RectF-oval-float-startAngle-float-sweepAngle-画弧形" class="headerlink" title="arcTo(RectF oval, float startAngle, float sweepAngle, boolean forceMoveTo) / arcTo(float left, float top, float right, float bottom, float startAngle, float sweepAngle, boolean forceMoveTo) / arcTo(RectF oval, float startAngle, float sweepAngle) 画弧形"></a>arcTo(RectF oval, float startAngle, float sweepAngle, boolean forceMoveTo) / arcTo(float left, float top, float right, float bottom, float startAngle, float sweepAngle, boolean forceMoveTo) / arcTo(RectF oval, float startAngle, float sweepAngle) 画弧形</h6><p>这个方法和 <code>Canvas.drawArc()</code> 比起来，少了一个参数 <code>useCenter</code>，而多了一个参数 <code>forceMoveTo</code> 。</p><p>少了 <code>useCenter</code> ，是因为 <code>arcTo()</code> 只用来画弧形而不画扇形，所以不再需要 <code>useCenter</code> 参数；而多出来的这个 <code>forceMoveTo</code> 参数的意思是，绘制是要「抬一下笔移动过去」，还是「直接拖着笔过去」，区别在于是否留下移动的痕迹。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.lineTo(<span class="number">100</span>, <span class="number">100</span>);  </span><br><span class="line">path.arcTo(<span class="number">100</span>, <span class="number">100</span>, <span class="number">300</span>, <span class="number">300</span>, <span class="number">-90</span>, <span class="number">90</span>, true); <span class="comment">// 强制移动到弧形起点（无痕迹）</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7yl6oc1j30ah07j0so.jpg" alt=""></p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.lineTo(<span class="number">100</span>, <span class="number">100</span>);  </span><br><span class="line">path.arcTo(<span class="number">100</span>, <span class="number">100</span>, <span class="number">300</span>, <span class="number">300</span>, <span class="number">-90</span>, <span class="number">90</span>, false); <span class="comment">// 直接连线连到弧形起点（有痕迹）</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7z1kr1ij309h06xq2v.jpg" alt=""></p><h6 id="addArc-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-addArc-RectF-oval-float-startAngle-float-sweepAngle"><a href="#addArc-float-left-float-top-float-right-float-bottom-float-startAngle-float-sweepAngle-addArc-RectF-oval-float-startAngle-float-sweepAngle" class="headerlink" title="addArc(float left, float top, float right, float bottom, float startAngle, float sweepAngle) / addArc(RectF oval, float startAngle, float sweepAngle)"></a>addArc(float left, float top, float right, float bottom, float startAngle, float sweepAngle) / addArc(RectF oval, float startAngle, float sweepAngle)</h6><p>又是一个弧形的方法。一个叫 <code>arcTo</code> ，一个叫 <code>addArc()</code>，都是弧形，区别在哪里？其实很简单： <code>addArc()</code> 只是一个直接使用了 <code>forceMoveTo = true</code> 的简化版 <code>arcTo()</code> 。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.lineTo(<span class="number">100</span>, <span class="number">100</span>);  </span><br><span class="line">path.addArc(<span class="number">100</span>, <span class="number">100</span>, <span class="number">300</span>, <span class="number">300</span>, <span class="number">-90</span>, <span class="number">90</span>);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7z9kur5j30ah07j0so.jpg" alt=""></p><h6 id="close-封闭当前子图形"><a href="#close-封闭当前子图形" class="headerlink" title="close() 封闭当前子图形"></a>close() 封闭当前子图形</h6><p>它的作用是把当前的子图形封闭，即由当前位置向当前子图形的起点绘制一条直线。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.moveTo(<span class="number">100</span>, <span class="number">100</span>);  </span><br><span class="line">path.lineTo(<span class="number">200</span>, <span class="number">100</span>);  </span><br><span class="line">path.lineTo(<span class="number">150</span>, <span class="number">150</span>);  </span><br><span class="line"><span class="comment">// 子图形未封闭</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7zidfmtj304p0330sl.jpg" alt=""></p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">paint.setStyle(Style.STROKE);  </span><br><span class="line">path.moveTo(<span class="number">100</span>, <span class="number">100</span>);  </span><br><span class="line">path.lineTo(<span class="number">200</span>, <span class="number">100</span>);  </span><br><span class="line">path.lineTo(<span class="number">150</span>, <span class="number">150</span>);  </span><br><span class="line">path.close(); <span class="comment">// 使用 close() 封闭子图形。等价于 path.lineTo(100, 100)</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7zqj7llj304n03aglh.jpg" alt=""></p><p><code>close()</code> 和 <code>lineTo(起点坐标)</code> 是完全等价的。</p><blockquote><p>「子图形」：官方文档里叫做 <code>contour</code> 。但由于在这个场景下我找不到这个词合适的中文翻译（直译的话叫做「轮廓」），所以我换了个便于中国人理解的词：「子图形」。前面说到，第一组方法是「添加子图形」，所谓「子图形」，指的就是一次不间断的连线。一个 <code>Path</code> 可以包含多个子图形。当使用第一组方法，即 <code>addCircle()</code> <code>addRect()</code> 等方法的时候，每一次方法调用都是新增了一个独立的子图形；而如果使用第二组方法，即 <code>lineTo()</code> <code>arcTo()</code> 等方法的时候，则是每一次断线（即每一次「抬笔」），都标志着一个子图形的结束，以及一个新的子图形的开始。</p><p>另外，不是所有的子图形都需要使用 <code>close()</code> 来封闭。当<strong>需要填充图形时（即 <code>Paint.Style</code> 为 <code>FILL</code> 或 <code>FILL_AND_STROKE</code>）</strong>，<code>Path</code> 会自动封闭子图形。</p><p><code>java paint.setStyle(Style.FILL); path.moveTo(100, 100); path.lineTo(200, 100); path.lineTo(150, 150); // 这里只绘制了两条边，但由于 Style 是 FILL ，所以绘制时会自动封口</code></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig80a2v95j304z035glh.jpg" alt=""></p></blockquote><p>以上就是 <code>Path</code> 的第一类方法：直接描述路径的。</p><h4 id="Path-方法第二类：辅助的设置或计算"><a href="#Path-方法第二类：辅助的设置或计算" class="headerlink" title="Path 方法第二类：辅助的设置或计算"></a>Path 方法第二类：辅助的设置或计算</h4><p>这类方法的使用场景比较少，我在这里就不多讲了，只讲其中一个方法： <code>setFillType(FillType fillType)</code>。</p><h5 id="Path-setFillType-Path-FillType-ft-设置填充方式"><a href="#Path-setFillType-Path-FillType-ft-设置填充方式" class="headerlink" title="Path.setFillType(Path.FillType ft) 设置填充方式"></a>Path.setFillType(Path.FillType ft) 设置填充方式</h5><p>前面在说 <code>dir</code> 参数的时候提到， <code>Path.setFillType(fillType)</code> 是用来设置图形自相交时的填充算法的：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig8042q9jj30hb0aodg6.jpg" alt=""></p><p>方法中填入不同的 <code>FillType</code> 值，就会有不同的填充效果。<code>FillType</code> 的取值有四个：</p><ul><li><code>EVEN_ODD</code></li><li><code>WINDING</code> （默认值）</li><li><code>INVERSE_EVEN_ODD</code></li><li><code>INVERSE_WINDING</code></li></ul><p>其中后面的两个带有 <code>INVERSE_</code> 前缀的，只是前两个的反色版本，所以只要把前两个，即 <code>EVEN_ODD</code> 和 <code>WINDING</code>，搞明白就可以了。</p><p><code>EVEN_ODD</code> 和 <code>WINDING</code> 的原理有点复杂，直接讲出来的话信息量太大，所以我先给一个简单粗暴版的总结，你感受一下： <code>WINDING</code> 是「全填充」，而 <code>EVEN_ODD</code> 是「交叉填充」：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig7zyzjtrj30kx0jugn9.jpg" alt=""></p><p>之所以叫「简单粗暴版」，是因为这些只是通常情形下的效果；而如果要准确了解它们在所有情况下的效果，就得先知道它们的原理，即它们的具体算法。</p><h4 id="EVEN-ODD-和-WINDING-的原理"><a href="#EVEN-ODD-和-WINDING-的原理" class="headerlink" title="EVEN_ODD 和 WINDING 的原理"></a>EVEN_ODD 和 WINDING 的原理</h4><h5 id="EVEN-ODD"><a href="#EVEN-ODD" class="headerlink" title="EVEN_ODD"></a>EVEN_ODD</h5><p>即 even-odd rule （奇偶原则）：对于平面中的任意一点，向任意方向射出一条射线，这条射线和图形相交的次数（相交才算，相切不算哦）如果是奇数，则这个点被认为在图形内部，是要被涂色的区域；如果是偶数，则这个点被认为在图形外部，是不被涂色的区域。还以左右相交的双圆为例：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig811a7tnj30jj06h74p.jpg" alt=""></p><blockquote><p>射线的方向无所谓，同一个点射向任何方向的射线，结果都是一样的，不信你可以试试。</p></blockquote><p>从上图可以看出，射线每穿过图形中的一条线，内外状态就发生一次切换，这就是为什么 <code>EVEN_ODD</code> 是一个「交叉填充」的模式。</p><h5 id="WINDING"><a href="#WINDING" class="headerlink" title="WINDING"></a>WINDING</h5><p>即 non-zero winding rule （非零环绕数原则）：首先，它需要你图形中的所有线条都是有绘制方向的：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig81dw60ej307b0bm3yq.jpg" alt=""></p><p>然后，同样是从平面中的点向任意方向射出一条射线，但计算规则不一样：以 0 为初始值，对于射线和图形的所有交点，遇到每个顺时针的交点（图形从射线的左边向右穿过）把结果加 1，遇到每个逆时针的交点（图形从射线的右边向左穿过）把结果减 1，最终把所有的交点都算上，得到的结果如果不是 0，则认为这个点在图形内部，是要被涂色的区域；如果是 0，则认为这个点在图形外部，是不被涂色的区域。</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig81scem2j30k90digmo.jpg" alt=""></p><blockquote><p>和 <code>EVEN_ODD</code> 相同，射线的方向并不影响结果。</p></blockquote><p>所以，我前面的那个「简单粗暴」的总结，对于 <code>WINDING</code> 来说并不完全正确：如果你所有的图形都用相同的方向来绘制，那么 <code>WINDING</code> 确实是一个「全填充」的规则；但如果使用不同的方向来绘制图形，结果就不一样了。</p><blockquote><p>图形的方向：对于添加子图形类方法（如 <code>Path.addCircle()</code> <code>Path.addRect()</code>）的方向，由方法的 <code>dir</code> 参数来控制，这个在前面已经讲过了；而对于画线类的方法（如 <code>Path.lineTo()``Path.arcTo()</code>）就更简单了，线的方向就是图形的方向。</p></blockquote><p>所以，完整版的 <code>EVEN_ODD</code> 和 <code>WINDING</code> 的效果应该是这样的：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig820pdt3j30kw0ummzx.jpg" alt=""></p><p>而 <code>INVERSE_EVEN_ODD</code> 和 <code>INVERSE_WINDING</code> ，只是把这两种效果进行反转而已，你懂了 <code>EVEN_ODD</code> 和 <code>WINDING</code> ，自然也就懂 <code>INVERSE_EVEN_ODD</code> 和 <code>INVERSE_WINDING</code> 了，我就不讲了。</p><p>好，花了好长的篇幅来讲 <code>drawPath(path)</code> 和 <code>Path</code>，终于讲完了。同时， <code>Canvas</code> 对图形的绘制就也讲完了。图形简单时，使用 <code>drawCircle()</code> <code>drawRect()</code> 等方法来直接绘制；图形复杂时，使用 <code>drawPath()</code> 来绘制自定义图形。</p><p>除此之外， <code>Canvas</code> 还可以绘制 <code>Bitmap</code> 和文字。</p><h3 id="drawBitmap-Bitmap-bitmap-float-left-float-top-Paint-paint-画-Bitmap"><a href="#drawBitmap-Bitmap-bitmap-float-left-float-top-Paint-paint-画-Bitmap" class="headerlink" title="drawBitmap(Bitmap bitmap, float left, float top, Paint paint) 画 Bitmap"></a>drawBitmap(Bitmap bitmap, float left, float top, Paint paint) 画 Bitmap</h3><p>绘制 <code>Bitmap</code> 对象，也就是把这个 <code>Bitmap</code> 中的像素内容贴过来。其中 <code>left</code> 和 <code>top</code> 是要把 <code>bitmap</code> 绘制到的位置坐标。它的使用非常简单。</p><figure class="highlight lisp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">drawBitmap(<span class="name">bitmap</span>, <span class="number">200</span>, <span class="number">100</span>, paint)<span class="comment">;</span></span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig829pyhij306704fjrc.jpg" alt=""></p><p>它的重载方法：</p><p><code>drawBitmap(Bitmap bitmap, Rect src, RectF dst, Paint paint)</code> /<code>drawBitmap(Bitmap bitmap, Rect src, Rect dst, Paint paint)</code> /<code>drawBitmap(Bitmap bitmap, Matrix matrix, Paint paint)</code></p><blockquote><p><code>drawBitmap</code> 还有一个兄弟方法 <code>drawBitmapMesh()</code>，可以绘制具有网格拉伸效果的 Bitmap。 <code>drawBitmapMesh()</code> 的使用场景较少，所以不讲了，如果有兴趣你可以自己研究一下。</p></blockquote><h3 id="drawText-String-text-float-x-float-y-Paint-paint-绘制文字"><a href="#drawText-String-text-float-x-float-y-Paint-paint-绘制文字" class="headerlink" title="drawText(String text, float x, float y, Paint paint) 绘制文字"></a>drawText(String text, float x, float y, Paint paint) 绘制文字</h3><p>界面里所有的显示内容，都是绘制出来的，包括文字。 <code>drawText()</code> 这个方法就是用来绘制文字的。参数 <code>text</code> 是用来绘制的字符串，<code>x</code> 和 <code>y</code> 是绘制的起点坐标。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.drawText(text, <span class="number">200</span>, <span class="number">100</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig82jjn0fj304v018dfq.jpg" alt=""></p><h4 id="插播五：-Paint-setTextSize-float-textSize"><a href="#插播五：-Paint-setTextSize-float-textSize" class="headerlink" title="插播五： Paint.setTextSize(float textSize)"></a>插播五： Paint.setTextSize(float textSize)</h4><p>通过 <code>Paint.setTextSize(textSize)</code>，可以设置文字的大小。</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">paint.setTextSize(<span class="number">18</span>);  </span><br><span class="line">canvas.drawText(text, <span class="number">100</span>, <span class="number">25</span>, paint);  </span><br><span class="line">paint.setTextSize(<span class="number">36</span>);  </span><br><span class="line">canvas.drawText(text, <span class="number">100</span>, <span class="number">70</span>, paint);  </span><br><span class="line">paint.setTextSize(<span class="number">60</span>);  </span><br><span class="line">canvas.drawText(text, <span class="number">100</span>, <span class="number">145</span>, paint);  </span><br><span class="line">paint.setTextSize(<span class="number">84</span>);  </span><br><span class="line">canvas.drawText(text, <span class="number">100</span>, <span class="number">240</span>, paint);</span><br></pre></td></tr></table></figure><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig82r9e31j30ht085t9d.jpg" alt=""></p><blockquote><p>设置文字的位置和尺寸，这些只是绘制文字最基本的操作。文字的绘制具有极高的定制性，不过由于它的定制性实在太高了，所以我会在后面专门用一期来讲文字的绘制。这一期就不多讲了。</p></blockquote><p>嗯……就这样吧。绘制部分第一节， <code>Canvas</code> 的 <code>drawXXX()</code> 系列方法和 <code>Paint</code> 的基本使用，就到这里。</p><h2 id="练习项目"><a href="#练习项目" class="headerlink" title="练习项目"></a>练习项目</h2><p>为了避免转头就忘，强烈建议你趁热打铁，做一下这个练习项目：<a href="https://github.com/hencoder/PracticeDraw1" target="_blank" rel="noopener">HenCoderPracticeDraw1</a></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig835uympj30u50fudh8.jpg" alt=""></p><h2 id="下期预告"><a href="#下期预告" class="headerlink" title="下期预告"></a>下期预告</h2><p>下期是 <code>Paint</code> 的全面介绍，内容会比这期难，但也会更有趣，各位做好准备吧。最后再贴一次下节内容的部分图片作为预告：</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig83c7bxlj30ch045aa3.jpg" alt=""></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig83eeszuj30ia08gt9g.jpg" alt=""></p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig83h5vhlj30ij08iaaq.jpg" alt=""></p><h2 id="感谢"><a href="#感谢" class="headerlink" title="感谢"></a>感谢</h2><p>感谢参与这期预发布内测的读者：</p><p><a href="https://github.com/jiahuahuang" target="_blank" rel="noopener">jiahuaAndroid</a>、<a href="https://github.com/huajinyuan" target="_blank" rel="noopener">huajinyuan</a>、<a href="https://github.com/quhuainan" target="_blank" rel="noopener">quhuainan</a>、Van Gogh、停停走走、JK森、街 景、<a href="https://github.com/ChinaVolvocars" target="_blank" rel="noopener">Tiger</a>、意琦行_、<a href="https://github.com/MadisonRong" target="_blank" rel="noopener">MadisonRong</a>、<a href="https://github.com/haihaio" target="_blank" rel="noopener">AaronYi</a>、<a href="https://github.com/CzwAthou" target="_blank" rel="noopener">czwathou</a>、别对生活说抱歉、<a href="https://github.com/LuckLe" target="_blank" rel="noopener">hml</a>、四叶花</p><h2 id="订阅"><a href="#订阅" class="headerlink" title="订阅"></a>订阅</h2><p>由于网站的订阅功能依然没做好，所以可以先去我的<a href="https://zhuanlan.zhihu.com/hencoder" target="_blank" rel="noopener">知乎专栏</a>、<a href="https://juejin.im/user/552f20a7e4b060d72a89d87f" target="_blank" rel="noopener">掘金专栏</a>或微信公众号（HenCoder）订阅。微信公众号二维码： <img src="http://p1nxdiyfl.bkt.clouddn.com/006tNc79ly1fig83uuch7j30go0gognj.jpg" alt=""></p><h2 id="赞赏"><a href="#赞赏" class="headerlink" title="赞赏"></a>赞赏</h2>]]></content>
    
    <summary type="html">
    
      
      
        &lt;hr&gt;
&lt;hr&gt;
&lt;p&gt;#HenCoder Android 开发进阶: 自定义 View 1-1 绘制基础&lt;br&gt;从今天开始，HenCoder 就正式开讲知识技能了。按照我的计划，第一季是 UI，UI 一共分为三部分：绘制、布局和触摸反馈。本期是绘制部分的第一期。绘制大概会用 
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title></title>
    <link href="https://seachal.me/uncategorized/2017/12/30/2017-12-30-1/"/>
    <id>https://seachal.me/uncategorized/2017/12/30/2017-12-30-1/</id>
    <published>2017-12-30T13:33:22.000Z</published>
    <updated>2017-12-30T13:33:22.000Z</updated>
    
    <content type="html"><![CDATA[<hr><p>layout：    布局<br>title：     标题<br>date：    建立日期    文件建立日期<br>updated：    更新日期    文件更新日期<br>comments    ：开启文章的评论功能    true<br>tags：    标签（不适用于分页）<br>categories：    分类（不适用于分页）</p><h2 id="permalink：-覆盖文章网址"><a href="#permalink：-覆盖文章网址" class="headerlink" title="permalink：    覆盖文章网址"></a>permalink：    覆盖文章网址</h2><p># </p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;hr&gt;
&lt;p&gt;layout：    布局&lt;br&gt;title：     标题&lt;br&gt;date：    建立日期    文件建立日期&lt;br&gt;updated：    更新日期    文件更新日期&lt;br&gt;comments    ：开启文章的评论功能    true&lt;br&gt;tags：  
      
    
    </summary>
    
    
  </entry>
  
  <entry>
    <title>Android设置TextView文显示固定字数 省略号表示多余部分</title>
    <link href="https://seachal.me/Android/2017/12/28/Android/Android%E8%AE%BE%E7%BD%AETextView%E6%96%87%E6%98%BE%E7%A4%BA%E5%9B%BA%E5%AE%9A%E5%AD%97%E6%95%B0%20%E7%9C%81%E7%95%A5%E5%8F%B7%E8%A1%A8%E7%A4%BA%E5%A4%9A%E4%BD%99%E9%83%A8%E5%88%86/"/>
    <id>https://seachal.me/Android/2017/12/28/Android/Android设置TextView文显示固定字数 省略号表示多余部分/</id>
    <published>2017-12-28T12:10:30.000Z</published>
    <updated>2017-12-29T11:55:38.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Android设置TextView文显示固定字数-省略号表示多余部分"><a href="#Android设置TextView文显示固定字数-省略号表示多余部分" class="headerlink" title="Android设置TextView文显示固定字数 省略号表示多余部分"></a>Android设置TextView文显示固定字数 省略号表示多余部分</h1><p>今天在设置TextView文显示固定字数 省略号表示多余部分时，发现<code>android:maxLines</code>和<code>android:ellipsize</code>这两个属性同时使用并没有效果。<br><em>原因：</em><br> 把               <code>android:layout_height=&quot;match_parent&quot;</code><br>改为                <code>android:layout_height=&quot;wrap_content&quot;</code><br>就有效果了。</p><p>##更多代码<br>在xml中：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">android:ellipsize=&quot;start&quot; 　　省略号在开头</span><br><span class="line">android:ellipsize=&quot;middle&quot;   省略号在中间</span><br><span class="line">android:ellipsize=&quot;marquee&quot;  跑马灯</span><br></pre></td></tr></table></figure><p>在java文件中：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tv.setEllipsize(TextUtils.TruncateAt.valueOf(&quot;START&quot;));</span><br><span class="line">tv.setEllipsize(TextUtils.TruncateAt.valueOf(&quot;MIDDLE&quot;));</span><br><span class="line">tv.setEllipsize(TextUtils.TruncateAt.valueOf(&quot;MARQUEE&quot;));</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Android设置TextView文显示固定字数-省略号表示多余部分&quot;&gt;&lt;a href=&quot;#Android设置TextView文显示固定字数-省略号表示多余部分&quot; class=&quot;headerlink&quot; title=&quot;Android设置TextView文显示固定字数
      
    
    </summary>
    
      <category term="Android" scheme="https://seachal.me/categories/Android/"/>
    
    
      <category term="Android" scheme="https://seachal.me/tags/Android/"/>
    
      <category term="TextView" scheme="https://seachal.me/tags/TextView/"/>
    
  </entry>
  
  <entry>
    <title>动态为TextView控件设置drawableLeft图标,并设置间距</title>
    <link href="https://seachal.me/Android/2017/12/28/Android/%E5%8A%A8%E6%80%81%E4%B8%BATextView%E6%8E%A7%E4%BB%B6%E8%AE%BE%E7%BD%AEdrawableLeft%E5%9B%BE%E6%A0%87,%E5%B9%B6%E8%AE%BE%E7%BD%AE%E9%97%B4%E8%B7%9D/"/>
    <id>https://seachal.me/Android/2017/12/28/Android/动态为TextView控件设置drawableLeft图标,并设置间距/</id>
    <published>2017-12-28T11:10:30.000Z</published>
    <updated>2017-12-30T14:09:40.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="动态为TextView控件设置drawableLeft图标-并设置间距"><a href="#动态为TextView控件设置drawableLeft图标-并设置间距" class="headerlink" title="动态为TextView控件设置drawableLeft图标,并设置间距"></a>动态为TextView控件设置drawableLeft图标,并设置间距</h1><p><em>重要属性:</em></p><p>textView.setCompoundDrawablePadding(4);//设置图片和text之间的间距 </p><p>textView.setPadding(-5, 0, 0, 0);//设置整体的padding</p><p>代码如下:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">addDrwable</span><span class="params">(TextView textView)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//获取图片</span></span><br><span class="line">       Drawable drawable = ContextCompat.getDrawable(mContext, R.drawable.staff_portrait);</span><br><span class="line">       drawable.setBounds(<span class="number">0</span>, <span class="number">0</span>, drawable.getMinimumWidth() / <span class="number">2</span>, drawable.getMinimumHeight() / <span class="number">2</span>);</span><br><span class="line">       <span class="comment">//在左侧添加图片</span></span><br><span class="line">       textView.setCompoundDrawables(drawable, <span class="keyword">null</span>, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">       <span class="comment">//textView.setCompoundDrawablesRelative(drawable, null, null, null);</span></span><br><span class="line">       textView.setCompoundDrawablePadding(<span class="number">5</span>);<span class="comment">//设置图片和text之间的间距</span></span><br><span class="line">       textView.setPadding(<span class="number">25</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure><p><strong>存在问题</strong><br>TextView的属性Gravity对添加的图片无效，如Gravity的值为center，图片会居左，文字相对于无图片时的位置偏右了一点。<br>所以只能被迫选择列表中的数据全部由居中给为居左</p><p><img src="http://p1nxdiyfl.bkt.clouddn.com/Jietu20171229-172723.png" alt="Jietu20171229-172723"></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;动态为TextView控件设置drawableLeft图标-并设置间距&quot;&gt;&lt;a href=&quot;#动态为TextView控件设置drawableLeft图标-并设置间距&quot; class=&quot;headerlink&quot; title=&quot;动态为TextView控件设置drawabl
      
    
    </summary>
    
      <category term="Android" scheme="https://seachal.me/categories/Android/"/>
    
    
      <category term="Android" scheme="https://seachal.me/tags/Android/"/>
    
      <category term="TextView" scheme="https://seachal.me/tags/TextView/"/>
    
      <category term="View" scheme="https://seachal.me/tags/View/"/>
    
  </entry>
  
  <entry>
    <title>Hexo创建文章和草稿</title>
    <link href="https://seachal.me/Bolg/2017/12/25/Blog%E5%88%9B%E5%BB%BA%E4%B8%8E%E4%BC%98%E5%8C%96/2017-12-30-1/"/>
    <id>https://seachal.me/Bolg/2017/12/25/Blog创建与优化/2017-12-30-1/</id>
    <published>2017-12-25T12:10:30.000Z</published>
    <updated>2017-12-25T12:10:30.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Hexo创建文章和草稿"><a href="#Hexo创建文章和草稿" class="headerlink" title="Hexo创建文章和草稿"></a>Hexo创建文章和草稿</h1><h2 id="文章"><a href="#文章" class="headerlink" title="文章"></a>文章</h2><p>命令行中输入：</p><p>========================</p><p>$ hexo new “new article”</p><p>========================</p><p>之后在source/_posts目录下面，多了一个new-article.md的文件。</p><p>打开之后我们会看到：</p><p>=========================</p><p>title: new article </p><p>date: 2014-11-01 20:10:33 </p><p>tags: </p><hr><p>=========================</p><p>文件的开头是属性，采用统一的yaml格式，用三条短横线分隔。下面是文章正文。</p><p>文章的正文支持markdown格式，建议你先学习一下它的语法。markdown不像html似的一大堆标签，很简单，只有几个符号。</p><p>新建、删除或修改文章后，不需要重启hexo server，刷新一下即可预览。</p><h2 id="属性"><a href="#属性" class="headerlink" title="属性"></a>属性</h2><p>文章可以拥有如下属性：</p><p>Setting Description Default</p><p>layout    Layout    post或page    </p><p>title    文章的标题    </p><p>date    创建日期    文件的创建日期    </p><p>updated    修改日期    文件的修改日期    </p><p>comments    是否开启评论    true    </p><p>tags    标签    </p><p>categories    分类    </p><p>permalink    url中的名字    文件名    </p><p>动态博客中通过发布文章页面设置的各种属性，在hexo里要这样设置。</p><h2 id="分类和标签"><a href="#分类和标签" class="headerlink" title="分类和标签"></a>分类和标签</h2><p>例如：</p><p>===========</p><p>categories: </p><ul><li>日记 </li></ul><p>tags: </p><ul><li><p>Hexo </p></li><li><p>node.js</p></li></ul><p>===========</p><h2 id="摘要"><a href="#摘要" class="headerlink" title="摘要"></a>摘要</h2><p>同wordpress一样，之上的内容为摘要。</p><h2 id="layout"><a href="#layout" class="headerlink" title="layout"></a>layout</h2><p>如果你修改了layout，在scaffolds文件夹里一定要有名字对应的模版文件，否则会采用默认模版。</p><h2 id="文件名"><a href="#文件名" class="headerlink" title="文件名"></a>文件名</h2><p>在配置文件中的new_post_name项可以设置文件名，默认为:title，也就是你在命令行输入的名字。</p><p>文件名可以为下面几个变量和字符串常量的任意组合：</p><p>Variable Description</p><p>:title    Escaped title (lower case and replace spaces with dash)    </p><p>:year    Created year (4-digit)    </p><p>:month    Created month (2-digit)    </p><p>:i_month    Created month (Without leading zeros)    </p><p>:day    Created day (2-digit)    </p><p>:i_day    Created day (Without leading zeros)    </p><h2 id="草稿"><a href="#草稿" class="headerlink" title="草稿"></a>草稿</h2><p>草稿相当于很多博客都有的“私密文章”功能。</p><p>============================</p><p>$ hexo new draft “new draft”</p><p>============================</p><p>会在source/_drafts目录下生成一个new-draft.md文件。但是这个文件不被显示在页面上，链接也访问不到。也就是说如果你想把某一篇文章移除显示，又不舍得删除，可以把它移动到_drafts目录之中。</p><p>如果你希望强行预览草稿，更改配置文件：</p><p>===================</p><p>render_drafts: true</p><p>===================</p><p>或者，如下方式启动server：</p><p>======================</p><p>$ hexo server –drafts</p><p>======================</p><p>下面这条命令可以把草稿变成文章，或者页面：</p><p>==================================</p><p>$ hexo publish [layout] </p><p>==================================</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Hexo创建文章和草稿&quot;&gt;&lt;a href=&quot;#Hexo创建文章和草稿&quot; class=&quot;headerlink&quot; title=&quot;Hexo创建文章和草稿&quot;&gt;&lt;/a&gt;Hexo创建文章和草稿&lt;/h1&gt;&lt;h2 id=&quot;文章&quot;&gt;&lt;a href=&quot;#文章&quot; class=&quot;head
      
    
    </summary>
    
      <category term="Bolg" scheme="https://seachal.me/categories/Bolg/"/>
    
    
      <category term="Hexo" scheme="https://seachal.me/tags/Hexo/"/>
    
      <category term="Blog" scheme="https://seachal.me/tags/Blog/"/>
    
  </entry>
  
  <entry>
    <title>如何在Mac下配置Github和Bitbucket的SSH</title>
    <link href="https://seachal.me/Github/2017/12/23/%E5%A6%82%E4%BD%95%E5%9C%A8Mac%E4%B8%8B%E9%85%8D%E7%BD%AEGithub%E5%92%8CBitbucket%E7%9A%84SSH/"/>
    <id>https://seachal.me/Github/2017/12/23/如何在Mac下配置Github和Bitbucket的SSH/</id>
    <published>2017-12-23T13:10:30.000Z</published>
    <updated>2017-12-29T11:55:46.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="如何在Mac下配置Github和Bitbucket的SSH"><a href="#如何在Mac下配置Github和Bitbucket的SSH" class="headerlink" title="如何在Mac下配置Github和Bitbucket的SSH"></a>如何在Mac下配置Github和Bitbucket的SSH</h1><h2 id="第一步：生成-SSH-公钥过程"><a href="#第一步：生成-SSH-公钥过程" class="headerlink" title="第一步：生成 SSH 公钥过程"></a>第一步：生成 SSH 公钥过程</h2><p>大多数 Git 服务器都会选择使用 SSH 公钥来进行授权。系统中的每个用户都必须提供一个公钥用于授权，没有的话就要生成一个。生成公钥的过程在所有操作系统上都差不多。 </p><h3 id="1-查看是否已经有了SSH密钥"><a href="#1-查看是否已经有了SSH密钥" class="headerlink" title="1.查看是否已经有了SSH密钥"></a>1.查看是否已经有了SSH密钥</h3><p> 首先先确认一下是否已经有一个公钥了。SSH 公钥默认储存在账户的主目录下的 <code>~/.ssh</code> 目录。进去看看：</p><figure class="highlight jboss-cli"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">cd</span> ~<span class="string">/.ssh</span></span><br></pre></td></tr></table></figure><p>如果没有密钥则不会有此文件夹，有则备份删除</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cp -R ~/<span class="selector-class">.ssh</span> ~/.ssh_bak</span><br><span class="line">rm -R ~/.ssh</span><br></pre></td></tr></table></figure><h3 id="2-生成密钥"><a href="#2-生成密钥" class="headerlink" title="2.生成密钥"></a>2.生成密钥</h3><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -<span class="built_in">t</span> rsa -C <span class="string">"你在Github上的邮箱"</span></span><br></pre></td></tr></table></figure><p>第一次要输入file名字，直接回车即可，默认文件名为<code>id_</code>前缀，然后会被要求输入个密码并且确认。</p><h2 id="第二步：添加密钥到SSH"><a href="#第二步：添加密钥到SSH" class="headerlink" title="第二步：添加密钥到SSH"></a>第二步：添加密钥到SSH</h2><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cd ~/.ssh</span><br><span class="line">ssh-<span class="keyword">add</span><span class="bash"> -K id_rsa</span></span><br></pre></td></tr></table></figure><p>需要刚才生成密钥时候输入的密码。</p><h3 id="在github上添加SSH-Key"><a href="#在github上添加SSH-Key" class="headerlink" title="在github上添加SSH Key"></a>在github上添加SSH Key</h3><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">more</span> <span class="selector-tag">id_rsa</span><span class="selector-class">.pub</span></span><br></pre></td></tr></table></figure><ul><li>查看公钥里的内容并且全部复制下来(包括开头ssh-rsa和结尾邮箱)。</li><li>进入到<code>github</code>的<code>setting</code>，找到SSH Keys页面添加一个key，title随意，赋值公钥进去保存即可。</li></ul><h2 id="第三步：测试是否成功"><a href="#第三步：测试是否成功" class="headerlink" title="第三步：测试是否成功"></a>第三步：测试是否成功</h2><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">ssh</span> <span class="selector-tag">git</span>@<span class="keyword">github</span>.<span class="keyword">com</span></span><br></pre></td></tr></table></figure><p>成功则返回类似:</p><figure class="highlight applescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Hi Seachal! You've successfully authenticated, <span class="keyword">but</span> GitHub <span class="keyword">does</span> <span class="keyword">not</span> provide shell access.</span><br><span class="line">Connection <span class="keyword">to</span> github.com closed.</span><br></pre></td></tr></table></figure><h3 id="Bitbucket"><a href="#Bitbucket" class="headerlink" title="Bitbucket"></a>Bitbucket</h3><p><a href="http://bicbucket.org/" target="_blank" rel="noopener">Bitbucket.org</a>是一个和<code>Github</code>极其类似的托管库网站，但是他的私有库是免费的，所以一般我们会把私有库放到<code>bitbucket</code>上，而其他库放到<code>github</code>上，毕竟<code>github</code>的<code>SNS</code>属性稍微多些，开源的库就尽量的希望有与其他人的更多互动。<br>给<code>bitbucket</code>添加SSH Key实际上是一样的。<br>测试bitbucket ssh是否可连接</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">ssh</span> <span class="selector-tag">-T</span> <span class="selector-tag">git</span>@<span class="keyword">bitbucket</span>.<span class="keyword">org</span></span><br></pre></td></tr></table></figure><p>成功则返回类似:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">logged in as Seachal.</span><br><span class="line"></span><br><span class="line">You can <span class="keyword">use</span> git <span class="keyword">or</span> hg <span class="keyword">to</span> <span class="keyword">connect</span> <span class="keyword">to</span> Bitbucket. Shell <span class="keyword">access</span> <span class="keyword">is</span> disabled.</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;如何在Mac下配置Github和Bitbucket的SSH&quot;&gt;&lt;a href=&quot;#如何在Mac下配置Github和Bitbucket的SSH&quot; class=&quot;headerlink&quot; title=&quot;如何在Mac下配置Github和Bitbucket的SSH&quot;&gt;&lt;/a
      
    
    </summary>
    
      <category term="Github" scheme="https://seachal.me/categories/Github/"/>
    
    
      <category term="Mac" scheme="https://seachal.me/tags/Mac/"/>
    
      <category term="Git" scheme="https://seachal.me/tags/Git/"/>
    
      <category term="Github" scheme="https://seachal.me/tags/Github/"/>
    
  </entry>
  
  <entry>
    <title>Android Studio快捷键_Mac和Windos和Linux</title>
    <link href="https://seachal.me/Android/2017/12/23/Android/Android%20Studio%E5%BF%AB%E6%8D%B7%E9%94%AE_Mac%E5%92%8CWindos%E5%92%8CLinux/"/>
    <id>https://seachal.me/Android/2017/12/23/Android/Android Studio快捷键_Mac和Windos和Linux/</id>
    <published>2017-12-23T08:30:30.000Z</published>
    <updated>2018-01-02T11:31:08.000Z</updated>
    
    <content type="html"><![CDATA[<p>Android Studio 为许多常用操作提供了键盘快捷键。 下表按操作系统显示了默认键盘快捷键，包括 Mac 、Windows、Linux 等系统。</p><table><thead><tr><th>说明</th><th>Mac</th><th>Windows/Linux</th></tr></thead><tbody><tr><td>常规</td><td>　</td><td>　</td></tr><tr><td>全部保存</td><td>Command + S</td><td>Control + S</td></tr><tr><td>同步</td><td>Command + Option + Y</td><td>Control + S</td></tr><tr><td>最大化/最小化编辑器</td><td>Control + Command + F12</td><td>Control + Shift + F12</td></tr><tr><td>添加到收藏夹</td><td>Option + Shift + F</td><td>Alt + Shift + F</td></tr><tr><td>使用当前配置文件检查当前文件</td><td>Option + Shift + I</td><td>Alt + Shift + I</td></tr><tr><td>快速切换模式</td><td>Control + `（反引号）</td><td>Control + `（反引号）</td></tr><tr><td>打开设置对话框</td><td>Command + ,（英文逗号）</td><td>Control + Alt + S</td></tr><tr><td>打开项目结构对话框</td><td>Command + ;（英文分号）</td><td>Control + Alt + Shift + S</td></tr><tr><td>在标签和工具窗口之间切换</td><td>Control + Tab</td><td>Control + Tab</td></tr><tr><td>在 Studio 内导航和搜索</td><td>　</td><td>　</td></tr><tr><td>搜索全部内容（包括代码和菜单）</td><td>按两次 Shift</td><td>按两次 Shift</td></tr><tr><td>查找</td><td>Command + F</td><td>Control + F</td></tr><tr><td>查找下一个</td><td>Command + G</td><td>F3</td></tr><tr><td>查找上一个</td><td>Command + Shift + G</td><td>Shift + F3</td></tr><tr><td>替换</td><td>Command + R</td><td>Control + R</td></tr><tr><td>查找操作</td><td>Command + Shift + A</td><td>Control + Shift + A</td></tr><tr><td>按符号名称搜索</td><td>Command + Option + O</td><td>Control + Alt + Shift + N</td></tr><tr><td>查找类</td><td>Command + O</td><td>Control + N</td></tr><tr><td>查找文件（而不是类）</td><td>Command + Shift + O</td><td>Control + Shift + N</td></tr><tr><td>在路径中查找</td><td>Command + Shift + F</td><td>Control + Shift + F</td></tr><tr><td>打开文件结构弹出式菜单</td><td>Command + F12</td><td>Control + F12</td></tr><tr><td>在打开的编辑器标签之前导航</td><td>Control + 向右/向左箭头</td><td>Alt + 向右/向左箭头</td></tr><tr><td>跳到源代码</td><td>F4 / Command + 向下箭头</td><td>F4 / Control + Enter</td></tr><tr><td>在新窗口中打开当前编辑器标签</td><td>Shift + F4</td><td>Shift + F4</td></tr><tr><td>最近打开的文件弹出式菜单</td><td>Command + E</td><td>Control + E</td></tr><tr><td>最近编辑的文件弹出式菜单</td><td>Command + Shift + E</td><td>Control + Shift + E</td></tr><tr><td>转到上一个编辑位置</td><td>Command + Shift + 退格键</td><td>Control + Shift + 退格键</td></tr><tr><td>关闭活动编辑器标签</td><td>Command + W</td><td>Control + F4</td></tr><tr><td>从工具窗口返回到编辑器窗口</td><td>Esc</td><td>Esc</td></tr><tr><td>隐藏活动或上一个活动工具窗口</td><td>Shift + Esc</td><td>Shift + Esc</td></tr><tr><td>转到行</td><td>Command + L</td><td>Control + G</td></tr><tr><td>打开类型层次结构</td><td>Control + H</td><td>Control + H</td></tr><tr><td>打开方法层次结构</td><td>Command + Shift + H</td><td>Control + Shift + H</td></tr><tr><td>打开调用层次结构</td><td>Control + Option + H</td><td>Control + Alt + H</td></tr><tr><td>编写代码</td><td>　</td><td>　</td></tr><tr><td>生成代码（getter、setter、构造函数、 hashCode/equals、toString、新文件、新类）</td><td>Command + N</td><td>Alt + Insert</td></tr><tr><td>替换方法</td><td>Control + O</td><td>Control + O</td></tr><tr><td>实现方法</td><td>Control + I</td><td>Control + I</td></tr><tr><td>控制语句 (if…else / try…catch / etc.)</td><td>Command + Option + T</td><td>Control + Alt + T</td></tr><tr><td>删除插入符处的行</td><td>Command + 退格键</td><td>Control + Y</td></tr><tr><td>折叠/展开当前代码块</td><td>Command + 减号/加号</td><td>Control + 减号/加号</td></tr><tr><td>折叠/展开所有代码块</td><td>Command + Shift + 减号/加号</td><td>Control + Shift + 减号/加号</td></tr><tr><td>复制当前行或选择</td><td>Command + D</td><td>Control + D</td></tr><tr><td>基本代码自动完成</td><td>Control + 空格键</td><td>Control + 空格键</td></tr><tr><td>智能代码自动完成（按预期 类型过滤方法和变量列表）</td><td>Control + Shift + 空格键</td><td>Control + Shift + 空格键</td></tr><tr><td>完成语句</td><td>Command + Shift + Enter</td><td>Control + Shift + Enter</td></tr><tr><td>快速文档查找</td><td>Control + J</td><td>Control + Q</td></tr><tr><td>显示选定方法的参数</td><td>Command + P</td><td>Control + P</td></tr><tr><td>转到声明（直接）</td><td>Command + B 或 Command + 点击</td><td>Control + B 或 Control + 点击</td></tr><tr><td>转到实现</td><td>Command + Alt + B</td><td>Control + Alt + B</td></tr><tr><td>转到超类方法/超类</td><td>Command + U</td><td>Control + U</td></tr><tr><td>打开快速定义查找</td><td>Command + Y</td><td>Control + Shift + I</td></tr><tr><td>切换项目工具窗口的可见性</td><td>Command + 1</td><td>Alt + 1</td></tr><tr><td>切换书签</td><td>F3</td><td>F11</td></tr><tr><td>通过助记切换书签</td><td>Option + F3</td><td>Control + F11</td></tr><tr><td>通过行注释添加注释/取消注释</td><td>Command + /</td><td>Control + /</td></tr><tr><td>通过块注释添加注释/取消注释</td><td>Command + Shift + /</td><td>Control + Shift + /</td></tr><tr><td>选择连续增加的代码块</td><td>Option + Up</td><td>Control + W</td></tr><tr><td>将当前选择退回到上一个状态</td><td>Option + Down</td><td>Control + Shift + W</td></tr><tr><td>移动到代码块起始位置</td><td>Option + Command + [</td><td>Control + [</td></tr><tr><td>移动到代码块结束位置</td><td>Option + Command + ]</td><td>Control + ]</td></tr><tr><td>从当前位置选择到代码块起始位置</td><td>Option + Command + Shift + [</td><td>Control + Shift + [</td></tr><tr><td>从当前位置选择到代码块结束位置</td><td>Option + Command + Shift + ]</td><td>Control + Shift + ]</td></tr><tr><td>从当前位置删除到单词结尾</td><td>Option + Delete</td><td>Control + Delete</td></tr><tr><td>从当前位置删除到单词开头</td><td>Option + 退格键</td><td>Control + 退格键</td></tr><tr><td>优化导入</td><td>Control + Option + O</td><td>Control + Alt + O</td></tr><tr><td>项目快速修复 （显示意图操作和快速修复）</td><td>Option + Enter</td><td>Alt + Enter</td></tr><tr><td>重新格式化代码</td><td>Command + Option + L</td><td>Control + Alt + L</td></tr><tr><td>自动缩进行</td><td>Control + Option + I</td><td>Control + Alt + I</td></tr><tr><td>缩进/取消缩进行</td><td>Tab/Shift + Tab</td><td>Tab/Shift + Tab</td></tr><tr><td>智能行合并</td><td>Control + Shift + J</td><td>Control + Shift + J</td></tr><tr><td>智能行拆分</td><td>Command + Enter</td><td>Control + Enter</td></tr><tr><td>开始新行</td><td>Shift + Enter</td><td>Shift + Enter</td></tr><tr><td>下一个/上一个突出显示的错误</td><td>F2 / Shift + F2</td><td>F2 / Shift + F2</td></tr><tr><td>构建并运行</td><td>　</td><td>　</td></tr><tr><td>构建</td><td>Command + F9</td><td>Control + F9</td></tr><tr><td>构建并运行</td><td>Control + R</td><td>Shift + F10</td></tr><tr><td>调试</td><td>　</td><td>　</td></tr><tr><td>调试</td><td>Control + D</td><td>Shift + F9</td></tr><tr><td>跳过</td><td>F8</td><td>F8</td></tr><tr><td>逐步执行</td><td>F7</td><td>F7</td></tr><tr><td>智能逐步执行</td><td>Shift + F7</td><td>Shift + F7</td></tr><tr><td>跳出</td><td>Shift + F8</td><td>Shift + F8</td></tr><tr><td>运行到光标位置</td><td>Option + F9</td><td>Alt + F9</td></tr><tr><td>评估表达式</td><td>Option + F8</td><td>Alt + F8</td></tr><tr><td>继续运行程序</td><td>Command + Option + R</td><td>F9</td></tr><tr><td>切换断点</td><td>Command + F8</td><td>Control + F8</td></tr><tr><td>查看断点</td><td>Command + Shift + F8</td><td>Control + Shift + F8</td></tr><tr><td>重构</td><td>　</td><td>　</td></tr><tr><td>复制</td><td>F5</td><td>F5</td></tr><tr><td>移动</td><td>F6</td><td>F6</td></tr><tr><td>安全删除</td><td>Command + Delete</td><td>Alt + Delete</td></tr><tr><td>重命名</td><td>Shift + F6</td><td>Shift + F6</td></tr><tr><td>更改签名</td><td>Command + F6</td><td>Control + F6</td></tr><tr><td>内联</td><td>Command + Option + N</td><td>Control + Alt + N</td></tr><tr><td>提取方法</td><td>Command + Option + M</td><td>Control + Alt + M</td></tr><tr><td>提取变量</td><td>Command + Option + V</td><td>Control + Alt + V</td></tr><tr><td>提取字段</td><td>Command + Option + F</td><td>Control + Alt + F</td></tr><tr><td>提取常量</td><td>Command + Option + C</td><td>Control + Alt + C</td></tr><tr><td>提取参数</td><td>Command + Option + P</td><td>Control + Alt + P</td></tr><tr><td>版本控制/本地历史记录</td><td>　</td><td>　</td></tr><tr><td>将项目提交到 VCS</td><td>Command + K</td><td>Control + K</td></tr><tr><td>从 VCS 更新项目</td><td>Command + T</td><td>Control + T</td></tr><tr><td>查看最近变更</td><td>Option + Shift + C</td><td>Alt + Shift + C</td></tr><tr><td>打开 VCS 弹出式菜单</td><td>Control + V</td><td>Alt + `（反引号）</td></tr></tbody></table>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;Android Studio 为许多常用操作提供了键盘快捷键。 下表按操作系统显示了默认键盘快捷键，包括 Mac 、Windows、Linux 等系统。&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;th&gt;Mac&lt;/th&gt;
&lt;th&gt;Window
      
    
    </summary>
    
      <category term="Android" scheme="https://seachal.me/categories/Android/"/>
    
    
      <category term="Mac" scheme="https://seachal.me/tags/Mac/"/>
    
      <category term="IDE" scheme="https://seachal.me/tags/IDE/"/>
    
      <category term="Android" scheme="https://seachal.me/tags/Android/"/>
    
  </entry>
  
</feed>
